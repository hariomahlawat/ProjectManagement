# READY TO PASTE — PRISM Publications Phase 30

Extract this ZIP into the ProjectManagement project root and overwrite the matching files.

**Important:** Phase 30 includes an EF Core migration:
`20261208180000_AddCompendiumCoverComposer.cs`

Then run:

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\tools\Test-PrismPublicationsPhase30.ps1
```

See `PRISM_Publications_Phase30_README.md` supplied alongside the ZIP for full implementation and validation notes.
