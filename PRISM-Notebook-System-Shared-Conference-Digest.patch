diff --git a/Pages/Notebook/Index.cshtml b/Pages/Notebook/Index.cshtml
index 14179ef..b89a41e 100644
--- a/Pages/Notebook/Index.cshtml
+++ b/Pages/Notebook/Index.cshtml
@@ -14,13 +14,14 @@
         "archive" or "archived" => "No archived items.",
         "completed" => "No completed items.",
         "trash" => "Trash is empty.",
+        "shared" => "Nothing shared with you yet.",
         _ => "No notebook items yet."
     };
     var emptyText = Model.Notebook.View switch
     {
         "labels" => "Add tags to notebook items, then browse by label here.",
         "today" or "overdue" or "reminders" => "Add a reminder to any note or checklist.",
-        "shared" => "Shared notes from other PRISM users will appear here.",
+        "shared" => "Items shared by PRISM or other users will appear here.",
         "archive" or "archived" => "Archived notes will appear here.",
         "completed" => "Completed notes will appear here.",
         "trash" => "Items moved to Trash will appear here until permanently deleted.",
@@ -49,7 +50,7 @@
         "today" => "Items due today.",
         "overdue" => "Past reminders that still need attention.",
         "reminders" => "All active reminders across your notebook.",
-        "shared" => "Notes and checklists shared with you by other PRISM users.",
+        "shared" => "Items shared with you by PRISM and other users.",
         "labels" when !string.IsNullOrWhiteSpace(Model.Notebook.Tag) => $"Items labelled {selectedLabelName}.",
         "labels" => "Browse and organise notebook labels.",
         "trash" => "Items in Trash are permanently deleted after 30 days.",
@@ -59,7 +60,7 @@
 }
 @section Styles { <link rel="stylesheet" href="~/css/notebook.css" asp-append-version="true" /> }
 
-<div class="notebook-shell editor-collapsed" data-board-view="grid" data-view="@Model.Notebook.View" data-current-user-id="@currentUserId" data-open-label-manager-on-load="@Model.EditLabels.ToString().ToLowerInvariant()">
+<div class="notebook-shell editor-collapsed" data-board-view="grid" data-view="@Model.Notebook.View" data-current-user-id="@currentUserId" data-system-shared-count="@Model.SystemSharedSurfaceCount" data-open-label-manager-on-load="@Model.EditLabels.ToString().ToLowerInvariant()">
     @* SECTION: Dedicated anti-forgery token for Notebook API mutations *@
     <div id="notebook-antiforgery-token" hidden>
         @Html.AntiForgeryToken()
@@ -157,13 +158,6 @@
             <button type="button" data-notebook-global-error-close aria-label="Dismiss error">×</button>
         </div>
 
-        @* SECTION: Live command digest. This is a virtual read-only note and is not
-           counted, sorted, archived or persisted with user Notebook items. *@
-        @if (Model.ConferenceDigest is { TotalDirectionCount: > 0 } conferenceDigest)
-        {
-            <partial name="_NotebookConferenceDigest" model="conferenceDigest" />
-        }
-
         @* SECTION: Card-first notebook board *@
         @if (Model.Notebook.View == "labels" && string.IsNullOrWhiteSpace(Model.Notebook.Tag))
         {
@@ -185,8 +179,7 @@
         {
 
             var hasAnyHomeItems = Model.Notebook.PinnedItems.Any()
-                || Model.Notebook.OtherItems.Any()
-                || Model.ConferenceDigest is { TotalDirectionCount: > 0 };
+                || Model.Notebook.OtherItems.Any();
 
             @* SECTION: Manual-order friendly home board sections *@
             <section class="notebook-board-section" data-notebook-section="pinned" hidden="@(!Model.Notebook.PinnedItems.Any())">
@@ -212,6 +205,42 @@
                 }
             </section>
         }
+        else if (Model.Notebook.View == "shared")
+        {
+            var showPrismShared = Model.ShowConferenceDigest
+                && Model.ConferenceDigest is { TotalDirectionCount: > 0 };
+            var hasPeopleShared = Model.Notebook.Items.Any();
+
+            @* SECTION: System-shared surfaces are intentionally separated from person-to-person
+               collaboration. They use normal Notebook card geometry but remain live/read-only. *@
+            @if (showPrismShared && Model.ConferenceDigest is { } sharedDigest)
+            {
+                <section class="notebook-board-section notebook-shared-source-section" data-notebook-shared-source="prism">
+                    <div class="notebook-section-header"><h2>From PRISM</h2><span>1</span></div>
+                    <div class="notebook-board notebook-system-shared-board" data-layout="grid" data-item-count="1">
+                        <partial name="_NotebookConferenceDigest" model="sharedDigest" />
+                    </div>
+                </section>
+            }
+
+            @if (hasPeopleShared)
+            {
+                <section class="notebook-board-section notebook-shared-source-section" data-notebook-shared-source="people">
+                    <div class="notebook-section-header"><h2>From people</h2><span>@Model.Notebook.Items.Count</span></div>
+                    <div class="notebook-board" data-notebook-board data-layout-policy="masonry-always" data-layout="masonry" data-item-count="@Model.Notebook.Items.Count">
+                        @foreach (var item in Model.Notebook.Items)
+                        {
+                            <partial name="_NotebookCard" model="@(CardModelFactory.Create(item, new ProjectManagement.Services.Notebook.NotebookCardContext { View = Model.Notebook.View, Query = Model.Notebook.Query, Filter = Model.Notebook.Filter, Tag = Model.Notebook.Tag, SelectedId = null }))" />
+                        }
+                    </div>
+                </section>
+            }
+
+            @if (!showPrismShared && !hasPeopleShared)
+            {
+                <div class="notebook-empty" data-notebook-empty-state><h3>@emptyTitle</h3><p>@emptyText</p></div>
+            }
+        }
         else if (!(Model.Notebook.View == "labels" && string.IsNullOrWhiteSpace(Model.Notebook.Tag)))
         {
             <section class="notebook-board-section">
diff --git a/Pages/Notebook/Index.cshtml.cs b/Pages/Notebook/Index.cshtml.cs
index be0e964..898aa27 100644
--- a/Pages/Notebook/Index.cshtml.cs
+++ b/Pages/Notebook/Index.cshtml.cs
@@ -50,6 +50,8 @@ public class IndexModel : PageModel
 
     public NotebookIndexVm Notebook { get; private set; } = new();
     public ConferenceDirectionDigestVm? ConferenceDigest { get; private set; }
+    public bool ShowConferenceDigest { get; private set; }
+    public int SystemSharedSurfaceCount => ConferenceDigest is { TotalDirectionCount: > 0 } ? 1 : 0;
 
     public async Task<IActionResult> OnGetAsync(CancellationToken ct)
     {
@@ -82,20 +84,65 @@ public class IndexModel : PageModel
         NormalizeLegacyTypeView();
         Notebook = await _notebook.GetIndexAsync(userId, View, Query, Filter, Tag, selectedId: null, ct);
 
-        if (ShouldLoadConferenceDigest())
+        if (HasCommandNotebookRole())
         {
             ConferenceDigest = await _conferenceRead.GetLatestDirectionDigestAsync(userId, ct);
+            if (ConferenceDigest is { TotalDirectionCount: > 0 })
+            {
+                AddSystemSharedSurfaceToRail();
+                ShowConferenceDigest = ShouldShowConferenceDigest(ConferenceDigest);
+            }
         }
 
         return Page();
     }
 
-    private bool ShouldLoadConferenceDigest()
-        => string.Equals(View, "home", StringComparison.OrdinalIgnoreCase)
-            && string.IsNullOrWhiteSpace(Query)
-            && string.IsNullOrWhiteSpace(Filter)
-            && string.IsNullOrWhiteSpace(Tag)
-            && (User.IsInRole(RoleNames.Comdt) || User.IsInRole(RoleNames.HoD));
+    private bool HasCommandNotebookRole()
+        => User.IsInRole(RoleNames.Comdt) || User.IsInRole(RoleNames.HoD);
+
+    private void AddSystemSharedSurfaceToRail()
+    {
+        var shared = Notebook.RailItems.FirstOrDefault(item =>
+            string.Equals(item.Key, "shared", StringComparison.OrdinalIgnoreCase));
+        if (shared is not null)
+        {
+            shared.Count += 1;
+        }
+    }
+
+    private bool ShouldShowConferenceDigest(ConferenceDirectionDigestVm digest)
+    {
+        if (!string.Equals(Notebook.View, "shared", StringComparison.OrdinalIgnoreCase)
+            || !string.IsNullOrWhiteSpace(Filter)
+            || !string.IsNullOrWhiteSpace(Tag))
+        {
+            return false;
+        }
+
+        if (string.IsNullOrWhiteSpace(Query))
+        {
+            return true;
+        }
+
+        var term = Query.Trim();
+        if (Contains("Latest Conference Directions", term)
+            || Contains("Conference Review", term)
+            || Contains("PRISM", term)
+            || Contains("Command", term))
+        {
+            return true;
+        }
+
+        return digest.OfficerGroups.Any(group =>
+            Contains(group.OfficerDisplayName, term)
+            || group.Directions.Any(item =>
+                Contains(item.Title, term)
+                || Contains(item.DirectionText, term)));
+    }
+
+    private static bool Contains(string? value, string term)
+        => !string.IsNullOrWhiteSpace(value)
+            && value.Contains(term, StringComparison.OrdinalIgnoreCase);
 
     private void NormalizeLegacyTypeView()
     {
diff --git a/Pages/Notebook/_NotebookConferenceDigest.cshtml b/Pages/Notebook/_NotebookConferenceDigest.cshtml
index bc688ae..a7e83c9 100644
--- a/Pages/Notebook/_NotebookConferenceDigest.cshtml
+++ b/Pages/Notebook/_NotebookConferenceDigest.cshtml
@@ -8,9 +8,8 @@
     var previewTakes = new int[previewGroups.Length];
     var remainingPreviewSlots = previewDirectionLimit;
 
-    // Preserve PO-wise context in the compact card: when two officers are available,
-    // reserve at least one preview slot for the second officer instead of allowing the
-    // first officer to consume the entire preview.
+    // Preserve PO context in the compact note: when two officers are available,
+    // reserve at least one preview slot for the second officer.
     for (var index = 0; index < previewGroups.Length && remainingPreviewSlots > 0; index++)
     {
         var groupsAfter = previewGroups.Length - index - 1;
@@ -27,83 +26,62 @@
         remainingPreviewSlots -= take;
     }
 
-    var hiddenOfficerGroups = Model.OfficerGroups.Skip(previewOfficerLimit).ToArray();
-    var hiddenOfficerCount = hiddenOfficerGroups.Length;
-    var hiddenOfficerDirectionCount = hiddenOfficerGroups.Sum(group => group.Directions.Count);
+    var previewedDirectionCount = previewTakes.Sum();
+    var hiddenDirectionCount = Math.Max(0, Model.TotalDirectionCount - previewedDirectionCount);
 }
 
-<section class="notebook-board-section notebook-conference-digest" data-conference-digest aria-labelledby="notebook-command-digest-heading">
-    <div class="notebook-section-header notebook-conference-digest__section-header">
-        <h2 id="notebook-command-digest-heading"><i class="bi bi-shield-check" aria-hidden="true"></i>Command</h2>
-    </div>
-
-    <article class="notebook-conference-digest-card">
-        <button type="button"
-                class="notebook-conference-digest-card__open"
-                data-bs-toggle="modal"
-                data-bs-target="#notebookConferenceDigestModal"
-                aria-haspopup="dialog"
-                aria-label="Open latest conference directions">
-            <span class="notebook-conference-digest-card__topline">
-                <span>
-                    <strong>Latest Conference Directions</strong>
-                    <small><i class="bi bi-lock" aria-hidden="true"></i> System generated · Read only</small>
-                </span>
-                <span class="notebook-conference-digest-card__count" aria-label="@Model.TotalDirectionCount latest directions">@Model.TotalDirectionCount</span>
-            </span>
-
-            <span class="notebook-conference-digest-card__groups">
-                @for (var groupIndex = 0; groupIndex < previewGroups.Length; groupIndex++)
-                {
-                    var group = previewGroups[groupIndex];
-                    var previewTake = previewTakes[groupIndex];
-                    var hiddenInGroup = Math.Max(0, group.Directions.Count - previewTake);
-
-                    <span class="notebook-conference-digest-preview-group">
-                        <span class="notebook-conference-digest-preview-group__heading">
-                            <strong>@group.OfficerDisplayName</strong>
-                            <small>@group.Directions.Count</small>
-                        </span>
-
-                        <span class="notebook-conference-digest-preview-group__items">
-                            @foreach (var item in group.Directions.Take(previewTake))
-                            {
-                                var issuedAtIst = IstClock.ToIst(item.IssuedAtUtc);
-                                <span class="notebook-conference-digest-preview-item">
-                                    <span class="notebook-conference-digest-preview-item__titleline">
-                                        <strong title="@item.Title">@item.Title</strong>
-                                        <time datetime="@item.IssuedAtUtc.ToString("O")">@issuedAtIst.ToString("dd MMM")</time>
-                                    </span>
-                                    <span class="notebook-conference-digest-preview-item__direction">@item.DirectionText</span>
-                                </span>
-                            }
-                        </span>
+@* SECTION: PRISM-shared virtual note. It deliberately uses the same geometry and
+   surface treatment as a normal Notebook card while remaining live and read-only. *@
+<article class="notebook-card notebook-card-color-white notebook-conference-shared-card"
+         data-notebook-system-shared-card="conference-directions">
+    <button type="button"
+            class="notebook-conference-shared-card__open"
+            data-bs-toggle="modal"
+            data-bs-target="#notebookConferenceDigestModal"
+            aria-haspopup="dialog"
+            aria-label="Open latest conference directions"></button>
+
+    <div class="notebook-card-body notebook-conference-shared-card__body">
+        <h3 class="notebook-card-title">Latest Conference Directions</h3>
+        <p class="notebook-card-preview notebook-conference-shared-card__summary">
+            @Model.TotalDirectionCount latest direction@(Model.TotalDirectionCount == 1 ? string.Empty : "s")
+            <span aria-hidden="true"> · </span>
+            @Model.OfficerCount Project Officer@(Model.OfficerCount == 1 ? string.Empty : "s")
+        </p>
+
+        <div class="notebook-conference-shared-card__groups">
+            @for (var groupIndex = 0; groupIndex < previewGroups.Length; groupIndex++)
+            {
+                var group = previewGroups[groupIndex];
+                var previewTake = previewTakes[groupIndex];
+
+                <div class="notebook-conference-shared-card__group">
+                    <div class="notebook-conference-shared-card__officer">
+                        <span title="@group.OfficerDisplayName">@group.OfficerDisplayName</span>
+                        <small>@group.Directions.Count</small>
+                    </div>
+                    @foreach (var item in group.Directions.Take(previewTake))
+                    {
+                        <div class="notebook-conference-shared-card__item">
+                            <strong title="@item.Title">@item.Title</strong>
+                            <span>@item.DirectionText</span>
+                        </div>
+                    }
+                </div>
+            }
+        </div>
 
-                        @if (hiddenInGroup > 0)
-                        {
-                            <span class="notebook-conference-digest-preview-group__more">+@hiddenInGroup more</span>
-                        }
-                    </span>
-                }
-            </span>
+        @if (hiddenDirectionCount > 0)
+        {
+            <div class="notebook-conference-shared-card__more">+@hiddenDirectionCount more direction@(hiddenDirectionCount == 1 ? string.Empty : "s")</div>
+        }
+    </div>
 
-            <span class="notebook-conference-digest-card__footer">
-                @if (hiddenOfficerCount > 0)
-                {
-                    <span>
-                        @hiddenOfficerCount more officer@(hiddenOfficerCount == 1 ? string.Empty : "s")
-                        <span aria-hidden="true"> · </span>
-                        @hiddenOfficerDirectionCount direction@(hiddenOfficerDirectionCount == 1 ? string.Empty : "s")
-                    </span>
-                }
-                else
-                {
-                    <span>@Model.OfficerCount Project Officer@(Model.OfficerCount == 1 ? string.Empty : "s")</span>
-                }
-                <span>View all @Model.TotalDirectionCount direction@(Model.TotalDirectionCount == 1 ? string.Empty : "s") <i class="bi bi-chevron-right" aria-hidden="true"></i></span>
-            </span>
-        </button>
-    </article>
+    <div class="notebook-sharing-summary notebook-conference-shared-card__sharing" aria-label="Shared by PRISM. Read only.">
+        <i class="bi bi-shield-lock" aria-hidden="true"></i>
+        <span>PRISM · Read only</span>
+    </div>
+</article>
 
     <div class="modal fade notebook-conference-digest-modal"
          id="notebookConferenceDigestModal"
@@ -114,7 +92,7 @@
             <div class="modal-content notebook-conference-digest-modal__content">
                 <header class="notebook-conference-digest-modal__header">
                     <div>
-                        <div class="notebook-conference-digest-modal__eyebrow">Command · System generated</div>
+                        <div class="notebook-conference-digest-modal__eyebrow">Command · PRISM</div>
                         <h2 id="notebookConferenceDigestModalLabel">Latest Conference Directions</h2>
                         <p>@Model.TotalDirectionCount latest direction@(Model.TotalDirectionCount == 1 ? string.Empty : "s") · @Model.OfficerCount Project Officer@(Model.OfficerCount == 1 ? string.Empty : "s")</p>
                     </div>
@@ -161,4 +139,3 @@
             </div>
         </div>
     </div>
-</section>
diff --git a/wwwroot/css/notebook.css b/wwwroot/css/notebook.css
index 70d3cb6..5be0544 100644
--- a/wwwroot/css/notebook.css
+++ b/wwwroot/css/notebook.css
@@ -4100,182 +4100,105 @@ body.notebook-confirm-open { overflow: hidden; }
     }
 }
 
-/* SECTION: Command conference digest
-   A virtual, read-only Notebook note for Comdt/HoD. It is intentionally outside the
-   sortable Notebook boards so it never affects user note counts or manual ordering. */
-.notebook-conference-digest {
-    margin-top: .28rem;
+/* SECTION: PRISM-shared Conference digest
+   The live command digest belongs in Shared with me and deliberately uses standard
+   Notebook card geometry. It is not part of the sortable/user-owned board model. */
+.notebook-shared-source-section {
+    margin-top: .5rem;
 }
 
-.notebook-conference-digest__section-header h2 {
-    display: inline-flex;
-    align-items: center;
-    gap: .38rem;
-    color: #334155;
-    font-weight: 600;
+.notebook-shared-source-section + .notebook-shared-source-section {
+    margin-top: .9rem;
 }
 
-.notebook-conference-digest__section-header h2 i {
-    color: #536ea8;
-    font-size: .8rem;
+.notebook-shell[data-board-view="grid"] .notebook-board.notebook-system-shared-board {
+    grid-template-columns: minmax(min(240px, 100%), 280px) !important;
+    grid-auto-rows: auto !important;
+    justify-content: start !important;
 }
 
-.notebook-conference-digest-card {
-    width: min(100%, 780px);
+.notebook-conference-shared-card {
+    position: relative;
+    isolation: isolate;
     overflow: hidden;
-    border: 1px solid #dce4ef;
-    border-radius: 12px;
-    background: linear-gradient(180deg, #ffffff 0%, #fbfcff 100%);
-    box-shadow: 0 2px 8px rgba(15, 23, 42, .05);
-    transition: border-color .16s ease, box-shadow .16s ease, transform .16s ease;
 }
 
-.notebook-conference-digest-card:hover,
-.notebook-conference-digest-card:focus-within {
-    border-color: #bdc9db;
-    box-shadow: 0 7px 18px rgba(15, 23, 42, .08);
+.notebook-conference-shared-card:hover,
+.notebook-conference-shared-card:focus-within {
+    border-color: rgba(71, 85, 105, .27);
+    box-shadow: 0 1px 2px rgba(15, 23, 42, .10), 0 5px 14px rgba(15, 23, 42, .09);
 }
 
-.notebook-conference-digest-card__open {
-    display: block;
+.notebook-conference-shared-card__open {
+    position: absolute;
+    inset: 0;
+    z-index: 3;
     width: 100%;
-    padding: .78rem .88rem .68rem;
     border: 0;
+    border-radius: inherit;
     background: transparent;
-    color: inherit;
-    text-align: left;
     cursor: pointer;
 }
 
-.notebook-conference-digest-card__open:focus-visible {
+.notebook-conference-shared-card__open:focus-visible {
     outline: 3px solid rgba(37, 99, 235, .18);
     outline-offset: -3px;
-    border-radius: 11px;
-}
-
-.notebook-conference-digest-card__topline,
-.notebook-conference-digest-card__footer,
-.notebook-conference-digest-preview-group__heading,
-.notebook-conference-digest-preview-item__titleline,
-.notebook-conference-digest-item__heading {
-    display: flex;
-    align-items: center;
-}
-
-.notebook-conference-digest-card__topline {
-    justify-content: space-between;
-    gap: .75rem;
-}
-
-.notebook-conference-digest-card__topline > span:first-child {
-    min-width: 0;
-}
-
-.notebook-conference-digest-card__topline strong {
-    display: block;
-    color: #111827;
-    font-size: .94rem;
-    font-weight: 600;
-    line-height: 1.25;
 }
 
-.notebook-conference-digest-card__topline small {
-    display: flex;
-    align-items: center;
-    gap: .3rem;
-    margin-top: .15rem;
-    color: #7c8798;
-    font-size: .69rem;
-    font-weight: 500;
+.notebook-conference-shared-card__body {
+    padding-bottom: 8px;
 }
 
-.notebook-conference-digest-card__count {
-    display: inline-flex;
-    flex: 0 0 auto;
-    align-items: center;
-    justify-content: center;
-    min-width: 1.72rem;
-    height: 1.72rem;
-    padding: 0 .38rem;
-    border-radius: 999px;
-    background: #edf2fb;
-    color: #49649a;
-    font-size: .74rem;
-    font-weight: 700;
+.notebook-conference-shared-card__summary {
+    margin-bottom: .58rem;
+    color: #66758a;
+    font-size: .75rem;
 }
 
-.notebook-conference-digest-card__groups {
+.notebook-conference-shared-card__groups {
     display: grid;
-    grid-template-columns: repeat(2, minmax(0, 1fr));
-    margin-top: .64rem;
-    border-top: 1px solid #edf1f6;
+    gap: .52rem;
 }
 
-.notebook-conference-digest-preview-group {
+.notebook-conference-shared-card__group {
     display: grid;
-    align-content: start;
     gap: .3rem;
     min-width: 0;
-    padding: .58rem .8rem .08rem 0;
-}
-
-.notebook-conference-digest-preview-group + .notebook-conference-digest-preview-group {
-    padding-right: 0;
-    padding-left: .8rem;
-    border-left: 1px solid #edf1f6;
 }
 
-.notebook-conference-digest-preview-group:only-child {
-    grid-column: 1 / -1;
-    padding-right: 0;
-}
-
-.notebook-conference-digest-preview-group__heading {
+.notebook-conference-shared-card__officer {
+    display: flex;
+    align-items: center;
     justify-content: space-between;
-    gap: .7rem;
-    padding-bottom: .25rem;
-    border-bottom: 1px solid #f0f3f7;
-}
-
-.notebook-conference-digest-preview-group__heading strong {
-    overflow: hidden;
+    gap: .5rem;
+    min-width: 0;
     color: #344054;
     font-size: .72rem;
     font-weight: 600;
-    letter-spacing: .01em;
+}
+
+.notebook-conference-shared-card__officer > span {
+    overflow: hidden;
     text-overflow: ellipsis;
     white-space: nowrap;
 }
 
-.notebook-conference-digest-preview-group__heading small {
+.notebook-conference-shared-card__officer small {
     flex: 0 0 auto;
     color: #8b96a8;
-    font-size: .67rem;
+    font-size: .66rem;
     font-weight: 500;
 }
 
-.notebook-conference-digest-preview-group__items {
+.notebook-conference-shared-card__item {
     display: grid;
-    gap: .18rem;
-    min-width: 0;
-}
-
-.notebook-conference-digest-preview-item {
-    display: grid;
-    min-width: 0;
-    padding: .13rem 0 .22rem;
-}
-
-.notebook-conference-digest-preview-item__titleline {
-    justify-content: space-between;
-    gap: .55rem;
+    gap: .03rem;
     min-width: 0;
 }
 
-.notebook-conference-digest-preview-item__titleline strong {
-    min-width: 0;
+.notebook-conference-shared-card__item strong {
     overflow: hidden;
-    color: #24324a;
+    color: #26344c;
     font-size: .78rem;
     font-weight: 500;
     line-height: 1.28;
@@ -4283,58 +4206,36 @@ body.notebook-confirm-open { overflow: hidden; }
     white-space: nowrap;
 }
 
-.notebook-conference-digest-preview-item__titleline time {
-    flex: 0 0 auto;
-    color: #8792a3;
-    font-size: .63rem;
-    font-weight: 500;
-    letter-spacing: .02em;
-    text-transform: uppercase;
-    white-space: nowrap;
-}
-
-.notebook-conference-digest-preview-item__direction {
+.notebook-conference-shared-card__item > span {
     display: -webkit-box;
-    margin-top: .06rem;
     overflow: hidden;
     color: #5f6f84;
-    font-size: .75rem;
+    font-size: .74rem;
     font-weight: 400;
-    line-height: 1.34;
+    line-height: 1.32;
     white-space: pre-line;
     -webkit-box-orient: vertical;
-    -webkit-line-clamp: 2;
-}
-
-.notebook-conference-digest-preview-group__more {
-    margin-top: .03rem;
-    color: #7a8799;
-    font-size: .66rem;
-    font-weight: 500;
+    -webkit-line-clamp: 1;
 }
 
-.notebook-conference-digest-card__footer {
-    justify-content: space-between;
-    gap: .75rem;
-    margin-top: .46rem;
-    padding-top: .5rem;
-    border-top: 1px solid #edf1f6;
-    color: #718096;
+.notebook-conference-shared-card__more {
+    margin-top: .45rem;
+    color: #758195;
     font-size: .69rem;
     font-weight: 500;
 }
 
-.notebook-conference-digest-card__footer > span:last-child {
-    display: inline-flex;
-    align-items: center;
-    gap: .18rem;
-    color: #49649a;
-    white-space: nowrap;
+.notebook-conference-shared-card__sharing {
+    position: relative;
+    z-index: 1;
+    max-width: calc(100% - 1rem);
+    margin-top: .12rem;
+    margin-bottom: 9px;
+    color: #6f7e92;
 }
 
-.notebook-shell[data-board-view="list"] .notebook-conference-digest-card {
-    width: min(100%, 840px);
-    margin-inline: auto;
+.notebook-conference-shared-card__sharing i {
+    color: #6077a5;
 }
 
 /* Expanded system note. Bootstrap supplies focus trapping/backdrop/escape semantics;
@@ -4537,25 +4438,6 @@ body.notebook-confirm-open { overflow: hidden; }
 }
 
 @media (max-width: 760px) {
-    .notebook-conference-digest-card {
-        width: 100%;
-    }
-
-    .notebook-conference-digest-card__groups {
-        grid-template-columns: minmax(0, 1fr);
-    }
-
-    .notebook-conference-digest-preview-group,
-    .notebook-conference-digest-preview-group + .notebook-conference-digest-preview-group {
-        padding: .55rem 0 .08rem;
-        border-left: 0;
-    }
-
-    .notebook-conference-digest-preview-group + .notebook-conference-digest-preview-group {
-        margin-top: .28rem;
-        border-top: 1px solid #edf1f6;
-    }
-
     .notebook-conference-digest-modal__dialog {
         width: calc(100vw - 1rem);
         margin: .5rem auto;
diff --git a/wwwroot/js/notebook/notebook-app.js b/wwwroot/js/notebook/notebook-app.js
index 1686362..128854a 100644
--- a/wwwroot/js/notebook/notebook-app.js
+++ b/wwwroot/js/notebook/notebook-app.js
@@ -68,14 +68,19 @@ export function initNotebookApp() {
   const globalError = document.querySelector('[data-notebook-global-error]');
   const globalErrorText = document.querySelector('[data-notebook-global-error-text]');
   const showGlobalError = (message) => { if (!globalError || !globalErrorText) { shell.dataset.error = message || 'Notebook action failed.'; return; } globalErrorText.textContent = message || 'Notebook action failed.'; globalError.hidden = false; };
+  const systemSharedCount = Math.max(0, Number.parseInt(shell.dataset.systemSharedCount || '0', 10) || 0);
   const applyCounts = (counts) => {
     if (!counts) return;
     Object.entries(counts).forEach(([key, value]) => {
+      const numericValue = Number(value) || 0;
+      const displayValue = key.toLowerCase() === 'shared'
+        ? numericValue + systemSharedCount
+        : numericValue;
       shell.querySelectorAll(`[data-notebook-count="${key}"]`).forEach((el) => {
-        el.textContent = String(value);
+        el.textContent = String(displayValue);
         if (key.toLowerCase() === 'overdue') {
           const isCurrentView = String(shell.dataset.view || '').toLowerCase() === 'overdue';
-          el.closest('.notebook-rail__item')?.toggleAttribute('hidden', Number(value) <= 0 && !isCurrentView);
+          el.closest('.notebook-rail__item')?.toggleAttribute('hidden', displayValue <= 0 && !isCurrentView);
         }
       });
     });
diff --git a/wwwroot/js/notebook/notebook-board.js b/wwwroot/js/notebook/notebook-board.js
index 2a10d78..0b45256 100644
--- a/wwwroot/js/notebook/notebook-board.js
+++ b/wwwroot/js/notebook/notebook-board.js
@@ -61,7 +61,7 @@ export function createNotebookBoard(root = document) {
       if (countEl) countEl.textContent = String(count);
     });
   };
-  const refreshEmptyState = () => { const empty = root.querySelector('[data-notebook-empty-state="current"]') || root.querySelector('[data-notebook-empty-state]') || root.querySelector('[data-notebook-empty]'); if (!empty) return; const count = [...root.querySelectorAll('[data-notebook-board]')].reduce((total, board) => total + board.querySelectorAll(':scope > [data-note-id]').length, 0); empty.hidden = count > 0; };
+  const refreshEmptyState = () => { const empty = root.querySelector('[data-notebook-empty-state="current"]') || root.querySelector('[data-notebook-empty-state]') || root.querySelector('[data-notebook-empty]'); if (!empty) return; const notebookCount = [...root.querySelectorAll('[data-notebook-board]')].reduce((total, board) => total + board.querySelectorAll(':scope > [data-note-id]').length, 0); const systemSharedCount = root.querySelectorAll('[data-notebook-system-shared-card]').length; empty.hidden = notebookCount + systemSharedCount > 0; };
 
   // SECTION: Card mutation helpers
   const upsertCard = (id, html, isPinned, options = {}) => { const current = findCard(id); const targetBoard = getBoard(isPinned); if (!targetBoard) throw new NotebookBoardTargetError(`Notebook board "${isPinned ? 'pinned' : 'others'}" was not found.`); const fragment = htmlToCardElement(html, id); const sameBoard = current && current.parentElement === targetBoard; const preservePosition = options.preservePosition !== false; if (sameBoard && preservePosition) { current.replaceWith(fragment); } else { current?.remove(); options.prepend === false ? targetBoard.append(fragment) : targetBoard.prepend(fragment); } refreshSectionVisibility(); refreshEmptyState(); return fragment; };
diff --git a/wwwroot/js/notebook/notebook-conference-digest-contract.test.js b/wwwroot/js/notebook/notebook-conference-digest-contract.test.js
index 6a254c1..41f7b18 100644
--- a/wwwroot/js/notebook/notebook-conference-digest-contract.test.js
+++ b/wwwroot/js/notebook/notebook-conference-digest-contract.test.js
@@ -4,63 +4,57 @@ const fs = require('node:fs');
 const path = require('node:path');
 
 const partialPath = path.join(__dirname, '../../../Pages/Notebook/_NotebookConferenceDigest.cshtml');
+const indexPath = path.join(__dirname, '../../../Pages/Notebook/Index.cshtml');
 const cssPath = path.join(__dirname, '../../css/notebook.css');
 
-function readPartial() {
-  return fs.readFileSync(partialPath, 'utf8');
-}
+const read = (file) => fs.readFileSync(file, 'utf8');
 
-function readCss() {
-  return fs.readFileSync(cssPath, 'utf8');
-}
+test('conference digest is a PRISM-shared normal-width notebook card', () => {
+  const partial = read(partialPath);
+  const index = read(indexPath);
+  const css = read(cssPath);
 
-test('conference digest preview is deliberately shallow and PO-wise', () => {
-  const partial = readPartial();
+  assert.match(partial, /class="notebook-card notebook-card-color-white notebook-conference-shared-card"/);
+  assert.match(partial, /PRISM · Read only/);
+  assert.match(index, /Model\.Notebook\.View == "shared"/);
+  assert.match(index, /<h2>From PRISM<\/h2><span>1<\/span>/);
+  assert.match(index, /<h2>From people<\/h2>/);
+  assert.match(css, /\.notebook-shell\[data-board-view="grid"\] \.notebook-board\.notebook-system-shared-board\s*\{[\s\S]*?280px\) !important;/);
+  assert.doesNotMatch(css, /\.notebook-conference-digest-card\s*\{[\s\S]*?780px/);
+});
+
+test('conference digest preview remains deliberately shallow and PO-wise', () => {
+  const partial = read(partialPath);
 
   assert.match(partial, /const int previewOfficerLimit = 2;/);
   assert.match(partial, /const int previewDirectionLimit = 3;/);
   assert.match(partial, /previewGroups = Model\.OfficerGroups\.Take\(previewOfficerLimit\)/);
-  assert.match(partial, /notebook-conference-digest-preview-group__more/);
-  assert.match(partial, /View all @Model\.TotalDirectionCount direction/);
+  assert.match(partial, /notebook-conference-shared-card__officer/);
+  assert.match(partial, /hiddenDirectionCount/);
 });
 
 test('conference digest does not repeat project idea or task type labels', () => {
-  const partial = readPartial();
+  const partial = read(partialPath);
 
   assert.doesNotMatch(partial, /@item\.KindLabel/);
   assert.doesNotMatch(partial, /notebook-conference-digest-item__kind/);
-  assert.doesNotMatch(partial, /notebook-conference-digest-preview-item__meta/);
-  assert.match(partial, /notebook-conference-digest-item__heading/);
-});
-
-test('conference digest card is wide and presents two PO previews side by side on desktop', () => {
-  const css = readCss();
-
-  assert.match(css, /\.notebook-conference-digest-card\s*\{[\s\S]*?width:\s*min\(100%,\s*780px\);/);
-  assert.match(css, /\.notebook-conference-digest-card__groups\s*\{[\s\S]*?grid-template-columns:\s*repeat\(2,\s*minmax\(0,\s*1fr\)\);/);
-  assert.match(css, /@media \(max-width: 760px\)[\s\S]*?grid-template-columns:\s*minmax\(0,\s*1fr\);/);
+  assert.doesNotMatch(partial, />PROJECT</);
+  assert.doesNotMatch(partial, />IDEA</);
+  assert.doesNotMatch(partial, />TASK</);
 });
 
-test('expanded digest uses a concise register row instead of type-specific cards', () => {
-  const css = readCss();
-
-  assert.match(css, /\.notebook-conference-digest-item__heading/);
-  assert.doesNotMatch(css, /\.notebook-conference-digest-item\.is-idea/);
-  assert.doesNotMatch(css, /\.notebook-conference-digest-item\.is-task/);
-  assert.doesNotMatch(css, /\.notebook-conference-digest-item__kind/);
-});
-
-test('expanded digest uses latest terminology and omits redundant per-officer direction counts', () => {
-  const partial = readPartial();
+test('expanded digest uses latest terminology and concise PO register hierarchy', () => {
+  const partial = read(partialPath);
 
   assert.match(partial, /@Model\.TotalDirectionCount latest direction/);
   assert.doesNotMatch(partial, /@Model\.TotalDirectionCount current direction/);
   assert.doesNotMatch(partial, /<span>@group\.Directions\.Count current direction/);
+  assert.match(partial, /Command · PRISM/);
   assert.match(partial, /Source: Conference Review/);
 });
 
-test('expanded digest separates PO groups without drawing rules between every direction', () => {
-  const css = readCss();
+test('expanded digest separates PO groups without rules between every direction', () => {
+  const css = read(cssPath);
   const officerRule = css.match(/\.notebook-conference-digest-officer\s*\{([^}]*)\}/)?.[1] ?? '';
   const itemRule = css.match(/\.notebook-conference-digest-item\s*\{([^}]*)\}/)?.[1] ?? '';
 
@@ -68,4 +62,3 @@ test('expanded digest separates PO groups without drawing rules between every di
   assert.doesNotMatch(itemRule, /border-bottom/);
   assert.match(css, /\.notebook-conference-digest-officer__items\s*\{[\s\S]*?gap:\s*\.48rem;/);
 });
-
diff --git a/wwwroot/js/notebook/notebook-conference-digest-typography-contract.test.js b/wwwroot/js/notebook/notebook-conference-digest-typography-contract.test.js
index 1f08404..4312c14 100644
--- a/wwwroot/js/notebook/notebook-conference-digest-typography-contract.test.js
+++ b/wwwroot/js/notebook/notebook-conference-digest-typography-contract.test.js
@@ -1,10 +1,9 @@
-import assert from 'node:assert/strict';
-import fs from 'node:fs';
-import path from 'node:path';
-import test from 'node:test';
-import { fileURLToPath } from 'node:url';
+const assert = require('node:assert/strict');
+const fs = require('node:fs');
+const path = require('node:path');
+const test = require('node:test');
 
-const here = path.dirname(fileURLToPath(import.meta.url));
+const here = __dirname;
 const cssPath = path.resolve(here, '../../css/notebook.css');
 const css = fs.readFileSync(cssPath, 'utf8');
 
@@ -23,15 +22,13 @@ test('conference digest uses restrained hierarchy for the expanded register', ()
     assert.match(rule('.notebook-conference-digest-item__heading time'), /font-weight:\s*400\s*;/);
 });
 
-test('compact digest follows the same PO-to-item-to-direction hierarchy', () => {
-    assert.match(rule('.notebook-conference-digest-preview-group__heading strong'), /font-weight:\s*600\s*;/);
-    assert.match(rule('.notebook-conference-digest-preview-item__titleline strong'), /font-weight:\s*500\s*;/);
-    assert.match(rule('.notebook-conference-digest-preview-item__direction'), /font-weight:\s*400\s*;/);
-    assert.match(rule('.notebook-conference-digest-preview-item__titleline time'), /font-weight:\s*500\s*;/);
+test('PRISM-shared compact card follows the same PO-to-item-to-direction hierarchy', () => {
+    assert.match(rule('.notebook-conference-shared-card__officer'), /font-weight:\s*600\s*;/);
+    assert.match(rule('.notebook-conference-shared-card__item strong'), /font-weight:\s*500\s*;/);
+    assert.match(rule('.notebook-conference-shared-card__item > span'), /font-weight:\s*400\s*;/);
 });
 
-test('supporting actions and metadata no longer compete with content', () => {
+test('supporting actions do not compete with content', () => {
     assert.match(rule('.notebook-conference-digest-officer__review-link'), /font-weight:\s*500\s*;/);
-    assert.match(rule('.notebook-conference-digest-card__topline small'), /font-weight:\s*500\s*;/);
-    assert.match(rule('.notebook-conference-digest-card__footer'), /font-weight:\s*500\s*;/);
+    assert.match(rule('.notebook-conference-shared-card__more'), /font-weight:\s*500\s*;/);
 });
diff --git a/mnt/data/prism_shared_after/wwwroot/js/notebook/notebook-system-shared-contract.test.js b/wwwroot/js/notebook/notebook-system-shared-contract.test.js
new file mode 100644
index 0000000..34c60c5
--- /dev/null
+++ b/wwwroot/js/notebook/notebook-system-shared-contract.test.js
@@ -0,0 +1,39 @@
+const test = require('node:test');
+const assert = require('node:assert/strict');
+const fs = require('node:fs');
+const path = require('node:path');
+
+const root = path.resolve(__dirname, '../../..');
+const index = fs.readFileSync(path.join(root, 'Pages/Notebook/Index.cshtml'), 'utf8');
+const pageModel = fs.readFileSync(path.join(root, 'Pages/Notebook/Index.cshtml.cs'), 'utf8');
+const app = fs.readFileSync(path.join(root, 'wwwroot/js/notebook/notebook-app.js'), 'utf8');
+const board = fs.readFileSync(path.join(root, 'wwwroot/js/notebook/notebook-board.js'), 'utf8');
+
+test('system-shared Conference digest is not rendered on All Notes', () => {
+  assert.doesNotMatch(index, /SECTION: Live command digest/);
+  assert.match(index, /else if \(Model\.Notebook\.View == "shared"\)/);
+  assert.match(index, /data-notebook-shared-source="prism"/);
+  assert.match(index, /var hasAnyHomeItems = Model\.Notebook\.PinnedItems\.Any\(\)\s*\|\| Model\.Notebook\.OtherItems\.Any\(\);/);
+});
+
+test('Shared with me count includes exactly one live PRISM surface when available', () => {
+  assert.match(index, /data-system-shared-count="@Model\.SystemSharedSurfaceCount"/);
+  assert.match(pageModel, /public int SystemSharedSurfaceCount => ConferenceDigest is \{ TotalDirectionCount: > 0 \} \? 1 : 0;/);
+  assert.match(pageModel, /shared\.Count \+= 1;/);
+  assert.match(app, /const systemSharedCount = Math\.max\(0, Number\.parseInt\(shell\.dataset\.systemSharedCount/);
+  assert.match(app, /key\.toLowerCase\(\) === 'shared'[\s\S]*?numericValue \+ systemSharedCount/);
+});
+
+test('system digest respects Shared view search and never participates in labels or type filters', () => {
+  assert.match(pageModel, /Notebook\.View, "shared"/);
+  assert.match(pageModel, /!string\.IsNullOrWhiteSpace\(Filter\)/);
+  assert.match(pageModel, /!string\.IsNullOrWhiteSpace\(Tag\)/);
+  assert.match(pageModel, /Contains\(group\.OfficerDisplayName, term\)/);
+  assert.match(pageModel, /Contains\(item\.Title, term\)/);
+  assert.match(pageModel, /Contains\(item\.DirectionText, term\)/);
+});
+
+test('client empty-state reconciliation counts PRISM-shared virtual cards', () => {
+  assert.match(board, /querySelectorAll\('\[data-notebook-system-shared-card\]'\)\.length/);
+  assert.match(board, /notebookCount \+ systemSharedCount > 0/);
+});
