@page "/Workspace"
@model ProjectManagement.Pages.Workspace.IndexModel
@{
    ViewData["Title"] = Model.IsCommandMode ? "Comdt / HoD Workspace" : "My Workspace";
    ViewData["UseFullWidth"] = true;
}
@section Styles {
    <link rel="stylesheet" href="~/css/workspace.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/command-workspace.css" asp-append-version="true" />
}

@if (Model.CanSwitchWorkspace)
{
    <nav class="workspace-mode-switcher" aria-label="Select workspace">
        <span class="workspace-mode-switcher__label">Workspace</span>
        <a asp-page="/Workspace/Index" asp-route-mode="command" class="@(Model.IsCommandMode ? "is-active" : null)">
            <i class="bi bi-building"></i><span>Comdt / HoD</span>
        </a>
        <a asp-page="/Workspace/Index" asp-route-mode="project-officer" class="@(!Model.IsCommandMode ? "is-active" : null)">
            <i class="bi bi-person-workspace"></i><span>Project Officer</span>
        </a>
    </nav>
}

@if (Model.IsCommandMode)
{
    @await Html.PartialAsync("_CommandWorkspace", Model)
}
else
{
    @await Html.PartialAsync("_ProjectOfficerWorkspace", Model)
}

@section Scripts {
    @if (Model.IsCommandMode)
    {
        <script src="~/lib/chart.js/chart.umd.js" asp-append-version="true"></script>
        <script type="module" src="~/js/pages/command-workspace.js" asp-append-version="true"></script>
    }
    else
    {
        <script src="~/js/pages/workspace-index.js" asp-append-version="true"></script>
    }
}
