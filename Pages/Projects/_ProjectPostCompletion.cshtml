@*
    Compatibility placeholder.

    The project Overview now uses one lifecycle-aware composition for active,
    completed, cancelled and legacy records. Post-completion content belongs in
    _ProjectCommandHeader, _ProjectLifecycleWorkspace and _ProjectWorkspaceHosts.
    This empty partial prevents older deployments from restoring the retired
    lower-page Transfer of Technology panel while replacement files are copied.
*@
