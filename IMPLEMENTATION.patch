diff -ruN baseline/Areas/ProjectOfficeReports/Pages/ARPP/Index.cshtml current/Areas/ProjectOfficeReports/Pages/ARPP/Index.cshtml
--- baseline/Areas/ProjectOfficeReports/Pages/ARPP/Index.cshtml	2026-07-31 02:14:42.809978255 +0000
+++ current/Areas/ProjectOfficeReports/Pages/ARPP/Index.cshtml	2026-07-31 02:14:43.881978229 +0000
@@ -8,7 +8,7 @@
     ViewData["ArppSection"] = "administration";
     ViewData["CanManageArpp"] = Model.CanManage;
     var allIssues = Model.Register.FinancialYears.SelectMany(group => group.Issues).ToArray();
-    var publishedRecordCount = allIssues.Count(issue => issue.HasPublishedSnapshot);
+    var activePublishedRecordCount = allIssues.Count(issue => issue.HasPublishedSnapshot);
     var workingRecordCount = allIssues.Count(issue => !issue.IsVerified);
 }
 
@@ -42,8 +42,8 @@
     }
 
     <section class="arpp-v2-summary" aria-label="Administration summary">
-        <article class="arpp-v2-summary__item"><span>ARPP records</span><strong>@Model.Register.TotalIssues</strong><small>@Model.Register.TotalEntries @(Model.Register.TotalEntries == 1 ? "row" : "rows") across all records</small></article>
-        <article class="arpp-v2-summary__item arpp-v2-summary__item--published"><span>Active published records</span><strong>@publishedRecordCount</strong><small>Includes unlocked records whose earlier revision remains published</small></article>
+        <article class="arpp-v2-summary__item"><span>ARPP records</span><strong>@Model.Register.TotalIssues</strong><small>@Model.Register.TotalEntries structured @(Model.Register.TotalEntries == 1 ? "row" : "rows") across @Model.Register.TotalIssues @(Model.Register.TotalIssues == 1 ? "record" : "records")</small></article>
+        <article class="arpp-v2-summary__item arpp-v2-summary__item--published"><span>Active published records</span><strong>@activePublishedRecordCount</strong><small>Includes unlocked records whose earlier revision remains published</small></article>
         <article class="arpp-v2-summary__item arpp-v2-summary__item--working"><span>Records under work</span><strong>@workingRecordCount</strong><small>Setup, correction or verification is in progress</small></article>
         <article class="arpp-v2-summary__item arpp-v2-summary__item--reconcile"><span>Reconciliation pending</span><strong>@Model.Register.UnlinkedEntries</strong><small>Document-row value @IndianCurrencyFormatter.FormatCompact(Model.Register.UnlinkedDocumentRowValue)</small></article>
     </section>
@@ -63,13 +63,37 @@
     {
         @foreach (var group in Model.Register.FinancialYears)
         {
+            var hasPublishedPosition = Model.PublishedPositions.TryGetValue(group.FinancialYearStart, out var publishedPosition);
+            var correctionCount = group.Issues.Count(issue => !issue.IsVerified && issue.HasPublishedSnapshot);
+            var underWorkCount = group.Issues.Count(issue => !issue.IsVerified);
+
             <section class="arpp-v2-year" aria-labelledby="arpp-year-@group.FinancialYearStart">
                 <header class="arpp-v2-year__header">
                     <div class="arpp-v2-year__title"><span>Financial year</span><strong id="arpp-year-@group.FinancialYearStart">@FinancialYearHelper.Format(group.FinancialYearStart)</strong></div>
                     <div class="arpp-v2-year__position">
-                        <span>@group.EntryCount @(group.EntryCount == 1 ? "document row" : "document rows")</span>
-                        <span><strong class="text-success-emphasis">@IndianCurrencyFormatter.FormatCompact(group.ApprovedLinkedIpaCost)</strong> approved · @group.ApprovedLinkedProjectCount @(group.ApprovedLinkedProjectCount == 1 ? "project" : "projects")</span>
-                        <span><strong class="text-warning-emphasis">@IndianCurrencyFormatter.FormatCompact(group.DelistedLinkedIpaCost)</strong> delisted · @group.DelistedLinkedProjectCount @(group.DelistedLinkedProjectCount == 1 ? "project" : "projects")</span>
+                        <span class="arpp-v2-year__record-count">@group.EntryCount structured @(group.EntryCount == 1 ? "row" : "rows") across @group.Issues.Count @(group.Issues.Count == 1 ? "record" : "records")</span>
+                        @if (hasPublishedPosition && publishedPosition is not null)
+                        {
+                            <span class="arpp-v2-year__position-label">Published position</span>
+                            <span><strong class="text-success-emphasis">@IndianCurrencyFormatter.FormatCompact(publishedPosition.ApprovedIpaValue)</strong> approved · @publishedPosition.ApprovedProjectCount @(publishedPosition.ApprovedProjectCount == 1 ? "project" : "projects")</span>
+                            <span><strong class="text-warning-emphasis">@IndianCurrencyFormatter.FormatCompact(publishedPosition.DelistedIpaValue)</strong> delisted · @publishedPosition.DelistedProjectCount @(publishedPosition.DelistedProjectCount == 1 ? "project" : "projects")</span>
+                            @if (publishedPosition.UnlinkedDocumentRowCount > 0)
+                            {
+                                <span class="arpp-v2-year__position-note">@publishedPosition.UnlinkedDocumentRowCount unlinked published @(publishedPosition.UnlinkedDocumentRowCount == 1 ? "row" : "rows")</span>
+                            }
+                            @if (correctionCount > 0)
+                            {
+                                <span class="arpp-v2-year__position-note">@correctionCount @(correctionCount == 1 ? "record" : "records") under correction</span>
+                            }
+                        }
+                        else
+                        {
+                            <span class="arpp-v2-year__position-empty">No published position</span>
+                            @if (underWorkCount > 0)
+                            {
+                                <span class="arpp-v2-year__position-note">@underWorkCount @(underWorkCount == 1 ? "record" : "records") under work</span>
+                            }
+                        }
                     </div>
                 </header>
 
diff -ruN baseline/Areas/ProjectOfficeReports/Pages/ARPP/Index.cshtml.cs current/Areas/ProjectOfficeReports/Pages/ARPP/Index.cshtml.cs
--- baseline/Areas/ProjectOfficeReports/Pages/ARPP/Index.cshtml.cs	2026-07-31 02:14:42.809978255 +0000
+++ current/Areas/ProjectOfficeReports/Pages/ARPP/Index.cshtml.cs	2026-07-31 02:14:43.361978242 +0000
@@ -10,13 +10,16 @@
 public sealed class IndexModel : PageModel
 {
     private readonly IArppReadService _readService;
+    private readonly IArppLibraryService _libraryService;
     private readonly IAuthorizationService _authorizationService;
 
     public IndexModel(
         IArppReadService readService,
+        IArppLibraryService libraryService,
         IAuthorizationService authorizationService)
     {
         _readService = readService ?? throw new ArgumentNullException(nameof(readService));
+        _libraryService = libraryService ?? throw new ArgumentNullException(nameof(libraryService));
         _authorizationService = authorizationService ?? throw new ArgumentNullException(nameof(authorizationService));
     }
 
@@ -31,6 +34,15 @@
 
     public bool CanManage { get; private set; }
 
+    /// <summary>
+    /// Authoritative, organisation-visible position derived only from verified published
+    /// snapshots. It is deliberately independent of working-copy values and free-text
+    /// administration filters so the financial-year heading never presents editable data
+    /// as the published position.
+    /// </summary>
+    public IReadOnlyDictionary<int, PublishedFinancialYearSummary> PublishedPositions { get; private set; }
+        = new Dictionary<int, PublishedFinancialYearSummary>();
+
     public async Task OnGetAsync(CancellationToken cancellationToken)
     {
         Register = await _readService.GetRegisterAsync(
@@ -38,9 +50,40 @@
             Query,
             cancellationToken);
 
+        var publishedPositions = new Dictionary<int, PublishedFinancialYearSummary>();
+        foreach (var group in Register.FinancialYears)
+        {
+            var currentPosition = await _libraryService.GetCurrentPositionAsync(
+                group.FinancialYearStart,
+                query: null,
+                cancellationToken);
+
+            if (currentPosition is null)
+            {
+                continue;
+            }
+
+            publishedPositions[group.FinancialYearStart] = new PublishedFinancialYearSummary(
+                currentPosition.ApprovedIpaValue,
+                currentPosition.DelistedIpaValue,
+                currentPosition.ApprovedRows.Count,
+                currentPosition.DelistedRows.Count,
+                currentPosition.TotalUnlinkedDocumentRows);
+        }
+
+        PublishedPositions = publishedPositions;
+
         CanManage = (await _authorizationService.AuthorizeAsync(
             User,
             resource: null,
             ProjectOfficeReportsPolicies.ManageArpp)).Succeeded;
     }
 }
+
+
+public sealed record PublishedFinancialYearSummary(
+    decimal ApprovedIpaValue,
+    decimal DelistedIpaValue,
+    int ApprovedProjectCount,
+    int DelistedProjectCount,
+    int UnlinkedDocumentRowCount);
diff -ruN baseline/ProjectManagement.Tests/Arpp/ArppAdministrationPublishedPositionContractTests.cs current/ProjectManagement.Tests/Arpp/ArppAdministrationPublishedPositionContractTests.cs
--- baseline/ProjectManagement.Tests/Arpp/ArppAdministrationPublishedPositionContractTests.cs	1970-01-01 00:00:00.000000000 +0000
+++ current/ProjectManagement.Tests/Arpp/ArppAdministrationPublishedPositionContractTests.cs	2026-07-31 02:14:43.985978226 +0000
@@ -0,0 +1,57 @@
+using Xunit;
+
+namespace ProjectManagement.Tests.Arpp;
+
+public sealed class ArppAdministrationPublishedPositionContractTests
+{
+    [Fact]
+    public void AdministrationRegister_UsesPublishedLibraryPosition_NotWorkingAggregates()
+    {
+        var pageModel = ReadRepoFile("Areas", "ProjectOfficeReports", "Pages", "ARPP", "Index.cshtml.cs");
+        var page = ReadRepoFile("Areas", "ProjectOfficeReports", "Pages", "ARPP", "Index.cshtml");
+
+        Assert.Contains("IArppLibraryService libraryService", pageModel, StringComparison.Ordinal);
+        Assert.Contains("GetCurrentPositionAsync", pageModel, StringComparison.Ordinal);
+        Assert.Contains("query: null", pageModel, StringComparison.Ordinal);
+        Assert.Contains("PublishedPositions", pageModel, StringComparison.Ordinal);
+
+        Assert.Contains("Published position", page, StringComparison.Ordinal);
+        Assert.Contains("publishedPosition.ApprovedIpaValue", page, StringComparison.Ordinal);
+        Assert.Contains("publishedPosition.DelistedIpaValue", page, StringComparison.Ordinal);
+        Assert.DoesNotContain("group.ApprovedLinkedIpaCost", page, StringComparison.Ordinal);
+        Assert.DoesNotContain("group.DelistedLinkedIpaCost", page, StringComparison.Ordinal);
+    }
+
+    [Fact]
+    public void AdministrationRegister_StatesRecordAndRowBasisPrecisely()
+    {
+        var page = ReadRepoFile("Areas", "ProjectOfficeReports", "Pages", "ARPP", "Index.cshtml");
+
+        Assert.Contains("structured", page, StringComparison.Ordinal);
+        Assert.Contains("across @group.Issues.Count", page, StringComparison.Ordinal);
+        Assert.Contains("No published position", page, StringComparison.Ordinal);
+        Assert.Contains("under correction", page, StringComparison.Ordinal);
+    }
+
+    private static string ReadRepoFile(params string[] relativePath)
+    {
+        var root = FindRepositoryRoot();
+        return File.ReadAllText(Path.Combine(new[] { root }.Concat(relativePath).ToArray()));
+    }
+
+    private static string FindRepositoryRoot()
+    {
+        var directory = new DirectoryInfo(AppContext.BaseDirectory);
+        while (directory is not null)
+        {
+            if (File.Exists(Path.Combine(directory.FullName, "ProjectManagement.csproj")))
+            {
+                return directory.FullName;
+            }
+
+            directory = directory.Parent;
+        }
+
+        throw new DirectoryNotFoundException("Could not locate the ProjectManagement repository root.");
+    }
+}
diff -ruN baseline/wwwroot/css/project-office-reports/arpp-redesign.css current/wwwroot/css/project-office-reports/arpp-redesign.css
--- baseline/wwwroot/css/project-office-reports/arpp-redesign.css	2026-07-31 02:14:42.809978255 +0000
+++ current/wwwroot/css/project-office-reports/arpp-redesign.css	2026-07-31 02:15:30.045977074 +0000
@@ -1243,3 +1243,67 @@
         padding: .45rem .6rem;
     }
 }
+
+/* Published-position semantics ------------------------------------- */
+.arpp-v2-year__position-label,
+.arpp-v2-year__position-empty {
+    color: var(--arpp-text-muted);
+    font-size: .65rem;
+    font-weight: 700;
+    letter-spacing: .055em;
+    text-transform: uppercase;
+}
+
+.arpp-v2-year__position-empty {
+    color: var(--arpp-text-secondary);
+}
+
+.arpp-v2-year__record-count,
+.arpp-v2-year__position-note {
+    color: var(--arpp-text-muted);
+    white-space: nowrap;
+}
+
+.arpp-v2-year__position-note {
+    padding-left: .85rem;
+    border-left: 1px solid var(--arpp-border);
+}
+
+@media (max-width: 1199.98px) {
+    .arpp-v2-year__position {
+        row-gap: .35rem;
+    }
+
+    .arpp-v2-year__position-note {
+        padding-left: 0;
+        border-left: 0;
+    }
+}
+
+@media (max-width: 767.98px) {
+    .arpp-v2-year__record-count,
+    .arpp-v2-year__position-note {
+        white-space: normal;
+    }
+}
+
+.arpp-v2-year__position-label,
+.arpp-v2-year__position-empty,
+.arpp-v2-year__record-count,
+.arpp-v2-year__position-note {
+    display: inline-flex;
+    grid-template-columns: none;
+    align-items: baseline;
+}
+
+.arpp-v2-year__position-label {
+    padding-left: .85rem;
+    border-left: 1px solid var(--arpp-border);
+}
+
+@media (max-width: 1199.98px) {
+    .arpp-v2-year__position-label {
+        padding-left: 0;
+        border-left: 0;
+    }
+}
