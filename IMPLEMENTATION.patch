--- a/Areas/ProjectOfficeReports/Pages/ARPP/Details.cshtml
+++ b/Areas/ProjectOfficeReports/Pages/ARPP/Details.cshtml
@@ -140,14 +140,11 @@
                     <article class="arpp-record-v2__metric"><span>Project linkage</span><strong>@Model.Issue.LinkedCount / @Model.Issue.Entries.Count</strong><small>@Model.Issue.UnlinkedCount pending</small></article>
                 </div>
 
-                <section class="arpp-record-v2__overview-section">
-                    <h3>Document identity</h3>
-                    <dl>
-                        <div><dt>Financial year</dt><dd>@FinancialYearHelper.Format(Model.Issue.FinancialYearStart)</dd></div>
-                        <div><dt>Issue type</dt><dd>@issueTypeLabel</dd></div>
-                        <div><dt>Issue date</dt><dd>@Model.Issue.IssueDate.ToString("dd MMM yyyy")</dd></div>
-                    </dl>
-                </section>
+                <div class="arpp-record-v2__identity-strip" aria-label="Document identity">
+                    <span><strong>Financial year</strong> @FinancialYearHelper.Format(Model.Issue.FinancialYearStart)</span>
+                    <span><strong>Issue type</strong> @issueTypeLabel</span>
+                    <span><strong>Issue date</strong> @Model.Issue.IssueDate.ToString("dd MMM yyyy")</span>
+                </div>
 
                 @if (Model.Issue.UnlinkedCount > 0 && Model.CanManage)
                 {
--- a/Areas/ProjectOfficeReports/Pages/ARPP/Index.cshtml
+++ b/Areas/ProjectOfficeReports/Pages/ARPP/Index.cshtml
@@ -29,10 +29,6 @@
         @if (Model.CanManage)
         {
             <div class="arpp-v2-page-header__actions">
-                <a asp-area="ProjectOfficeReports" asp-page="/ARPP/Reconcile" class="btn btn-outline-primary btn-sm">
-                    <span class="bi bi-link-45deg" aria-hidden="true"></span> Reconciliation
-                    @if (Model.Register.UnlinkedEntries > 0) { <span class="badge text-bg-primary">@Model.Register.UnlinkedEntries</span> }
-                </a>
                 <a asp-area="ProjectOfficeReports" asp-page="/ARPP/Create" class="btn btn-primary btn-sm"><span class="bi bi-plus-lg" aria-hidden="true"></span> New ARPP / Addendum</a>
             </div>
         }
@@ -46,8 +42,8 @@
 
     <section class="arpp-v2-summary" aria-label="Administration summary">
         <article class="arpp-v2-summary__item"><span>ARPP records</span><strong>@Model.Register.TotalIssues</strong><small>@Model.Register.TotalEntries recorded @(Model.Register.TotalEntries == 1 ? "row" : "rows")</small></article>
-        <article class="arpp-v2-summary__item arpp-v2-summary__item--published"><span>Published positions</span><strong>@publishedRecordCount</strong><small>Verified records and active published revisions</small></article>
-        <article class="arpp-v2-summary__item arpp-v2-summary__item--working"><span>Working copies</span><strong>@workingRecordCount</strong><small>Setup, correction or verification in progress</small></article>
+        <article class="arpp-v2-summary__item arpp-v2-summary__item--published"><span>Active published records</span><strong>@publishedRecordCount</strong><small>Includes unlocked records whose earlier revision remains published</small></article>
+        <article class="arpp-v2-summary__item arpp-v2-summary__item--working"><span>Records under work</span><strong>@workingRecordCount</strong><small>Setup, correction or verification is in progress</small></article>
         <article class="arpp-v2-summary__item arpp-v2-summary__item--reconcile"><span>Reconciliation pending</span><strong>@Model.Register.UnlinkedEntries</strong><small>Document-row value @IndianCurrencyFormatter.FormatCompact(Model.Register.UnlinkedDocumentRowValue)</small></article>
     </section>
 
--- a/Areas/ProjectOfficeReports/Pages/ARPP/Manage.cshtml
+++ b/Areas/ProjectOfficeReports/Pages/ARPP/Manage.cshtml
@@ -76,9 +76,10 @@
                     class="btn btn-outline-secondary arpp-workspace-v2__navigator-toggle"
                     data-arpp-navigator-toggle
                     aria-controls="arppWorkspaceNavigator"
-                    aria-expanded="false">
+                    aria-expanded="true"
+                    title="Show or hide the row navigator">
                 <span class="bi bi-layout-sidebar" aria-hidden="true"></span>
-                Navigator
+                <span>Rows</span>
             </button>
 
             <div class="arpp-workspace-v2__search" role="search">
@@ -88,7 +89,7 @@
                        type="search"
                        class="form-control"
                        autocomplete="off"
-                       placeholder="Find row, project, PPP or serial"
+                       placeholder="Find row or project"
                        data-arpp-jump-input
                        data-arpp-ui-only />
                 <button type="button" class="btn btn-outline-secondary" data-arpp-jump-next title="Find next match" aria-label="Find next matching row">
@@ -98,7 +99,7 @@
             </div>
 
             <button type="button"
-                    class="btn btn-outline-danger d-none"
+                    class="btn btn-outline-danger arpp-workspace-v2__issues-toggle d-none"
                     data-arpp-validation-toggle
                     aria-expanded="false"
                     aria-controls="arppWorkspaceIssues">
@@ -139,8 +140,8 @@
                         <span>Row navigator</span>
                         <strong data-arpp-visible-row-status>All rows</strong>
                     </div>
-                    <button type="button" class="btn btn-sm btn-light" data-arpp-navigator-close aria-label="Close row navigator">
-                        <span class="bi bi-x-lg" aria-hidden="true"></span>
+                    <button type="button" class="btn btn-sm btn-light" data-arpp-navigator-close aria-label="Collapse row navigator" title="Collapse row navigator">
+                        <span class="bi bi-chevron-left" aria-hidden="true"></span>
                     </button>
                 </div>
 
--- a/Areas/ProjectOfficeReports/Pages/ARPP/Print.cshtml
+++ b/Areas/ProjectOfficeReports/Pages/ARPP/Print.cshtml
@@ -15,6 +15,35 @@
         Model.Issue.CategorySummary[ArppCategory.CommittedLiability].TotalIpaCost +
         Model.Issue.CategorySummary[ArppCategory.CarryForward].TotalIpaCost;
     var delistedRowValue = Model.Issue.CategorySummary[ArppCategory.Delisted].TotalIpaCost;
+    var referenceDataReady = Model.Issue.Entries.All(entry => !entry.HasUnresolvedReferenceData);
+    var setupIncomplete = Model.Issue.Entries.Count == 0 || Model.Issue.Attachment is null;
+
+    string FormatPrintCurrency(decimal amount)
+    {
+        const decimal crore = 10_000_000m;
+        const decimal lakh = 100_000m;
+
+        if (Math.Abs(amount) >= crore)
+        {
+            return $"₹{amount / crore:N2} Cr";
+        }
+
+        if (Math.Abs(amount) >= lakh)
+        {
+            return $"₹{amount / lakh:N2} Lakh";
+        }
+
+        return $"₹{amount:N0}";
+    }
+
+    string CategoryShortLabel(ArppCategory category) => category switch
+    {
+        ArppCategory.New => "New",
+        ArppCategory.CommittedLiability => "CL",
+        ArppCategory.CarryForward => "CF",
+        ArppCategory.Delisted => "Delisted",
+        _ => ArppDisplayNames.For(category)
+    };
 }
 <!DOCTYPE html>
 <html lang="en">
@@ -41,53 +70,113 @@
         </header>
 
         <section class="arpp-print-summary" aria-label="Issue summary">
-            <div><span>Rows</span><strong>@Model.Issue.Entries.Count</strong></div>
-            <div><span>Approved row value</span><strong>@IndianCurrencyFormatter.FormatRupees(approvedRowValue)</strong><small>New, CL and CF</small></div>
-            <div><span>Delisted row value</span><strong>@IndianCurrencyFormatter.FormatRupees(delistedRowValue)</strong><small>Recorded separately</small></div>
-            <div><span>Project linkage</span><strong>@Model.Issue.LinkedCount linked</strong><small>@Model.Issue.UnlinkedCount pending</small></div>
-            <div class="arpp-print-summary__categories">
+            <div class="arpp-print-summary__metric">
+                <span>Rows</span>
+                <strong>@Model.Issue.Entries.Count</strong>
+                <small>Issued document rows</small>
+            </div>
+            <div class="arpp-print-summary__metric arpp-print-summary__metric--approved">
+                <span>Approved row value</span>
+                <strong>@FormatPrintCurrency(approvedRowValue)</strong>
+                <small>New, CL and CF</small>
+            </div>
+            <div class="arpp-print-summary__metric arpp-print-summary__metric--delisted">
+                <span>Delisted row value</span>
+                <strong>@FormatPrintCurrency(delistedRowValue)</strong>
+                <small>Recorded separately</small>
+            </div>
+            <div class="arpp-print-summary__metric">
+                <span>Project linkage</span>
+                <strong>@Model.Issue.LinkedCount / @Model.Issue.Entries.Count</strong>
+                <small>@Model.Issue.UnlinkedCount pending</small>
+            </div>
+
+            <div class="arpp-print-summary__categories" aria-label="Category breakdown">
                 @foreach (var category in Enum.GetValues<ArppCategory>())
                 {
                     var summary = Model.Issue.CategorySummary[category];
-                    <span><b>@ArppDisplayNames.For(category)</b> @summary.Count · @IndianCurrencyFormatter.FormatRupees(summary.TotalIpaCost)</span>
+                    <div>
+                        <span>@CategoryShortLabel(category)</span>
+                        <strong>@summary.Count</strong>
+                        <small>@FormatPrintCurrency(summary.TotalIpaCost)</small>
+                    </div>
                 }
             </div>
         </section>
 
-        <section class="arpp-print-source">
-            <strong>Record status:</strong>
-            <span>@(Model.Issue.IsVerified ? "Verified and locked" : "Unverified")</span>
-            @if (Model.Issue.IsVerified && Model.Issue.VerifiedAtUtc.HasValue)
+        <section class="arpp-print-source" aria-label="Record and source status">
+            @if (Model.Issue.IsVerified)
             {
-                <span> · Verified @IstClock.ToIst(Model.Issue.VerifiedAtUtc.Value).ToString("dd MMM yyyy, hh:mm tt") IST</span>
+                <div class="arpp-print-source__line">
+                    <strong>Record status:</strong>
+                    <span>Verified and published</span>
+                    @if (Model.Issue.VerifiedAtUtc.HasValue)
+                    {
+                        <span> · @IstClock.ToIst(Model.Issue.VerifiedAtUtc.Value).ToString("dd MMM yyyy, hh:mm tt") IST</span>
+                    }
+                    @if (!string.IsNullOrWhiteSpace(Model.Issue.VerifiedByDisplayName))
+                    {
+                        <span> · By @Model.Issue.VerifiedByDisplayName</span>
+                    }
+                </div>
             }
-            @if (Model.Issue.IsVerified && !string.IsNullOrWhiteSpace(Model.Issue.VerifiedByDisplayName))
+            else if (Model.Issue.HasPublishedSnapshot)
             {
-                <span> · By @Model.Issue.VerifiedByDisplayName</span>
-            }
-            <br />
-            <strong>Issued source PDF:</strong>
-            @if (Model.Issue.Attachment is null)
-            {
-                <span>Not attached in PRISM</span>
+                <div class="arpp-print-source__line">
+                    <strong>Working-copy status:</strong>
+                    <span>Unlocked for correction</span>
+                </div>
+                <div class="arpp-print-source__line">
+                    <strong>Published position:</strong>
+                    <span>Revision @(Model.Issue.PublishedRevisionNumber?.ToString() ?? "1") remains active</span>
+                    @if (Model.Issue.PublishedAtUtc.HasValue)
+                    {
+                        <span> · Published @IstClock.ToIst(Model.Issue.PublishedAtUtc.Value).ToString("dd MMM yyyy, hh:mm tt") IST</span>
+                    }
+                </div>
             }
             else
             {
-                <span>
-                    @Model.Issue.Attachment.OriginalFileName · @FileSizeFormatter.FormatFileSize(Model.Issue.Attachment.SizeBytes)
-                    · SHA-256 @(Model.Issue.Attachment.Sha256[..12])…
-                </span>
+                <div class="arpp-print-source__line">
+                    <strong>Working-copy status:</strong>
+                    <span>@(setupIncomplete ? "Setup incomplete" : !referenceDataReady ? "Reference review required" : "Ready for verification")</span>
+                </div>
             }
+
+            <div class="arpp-print-source__line">
+                <strong>Issued source PDF:</strong>
+                @if (Model.Issue.Attachment is null)
+                {
+                    <span>Not attached in PRISM</span>
+                }
+                else
+                {
+                    <span>
+                        @Model.Issue.Attachment.OriginalFileName · @FileSizeFormatter.FormatFileSize(Model.Issue.Attachment.SizeBytes)
+                        · SHA-256 @(Model.Issue.Attachment.Sha256[..Math.Min(12, Model.Issue.Attachment.Sha256.Length)])…
+                    </span>
+                }
+            </div>
         </section>
 
         <table class="arpp-print-table">
+            <colgroup>
+                <col class="arpp-print-col--number" />
+                <col class="arpp-print-col--serial" />
+                <col class="arpp-print-col--ppp" />
+                <col class="arpp-print-col--project" />
+                <col class="arpp-print-col--category" />
+                <col class="arpp-print-col--cost" />
+                <col class="arpp-print-col--cfa" />
+                <col class="arpp-print-col--fund" />
+                <col class="arpp-print-col--dfpds" />
+            </colgroup>
             <thead>
                 <tr>
                     <th class="number">#</th>
                     <th>Serial</th>
                     <th>PPP No.</th>
-                    <th>Project reference as issued</th>
-                    <th>PRISM project</th>
+                    <th>Project reference</th>
                     <th>Category</th>
                     <th class="money">IPA cost (₹)</th>
                     <th>CFA</th>
@@ -98,31 +187,37 @@
             <tbody>
                 @foreach (var entry in Model.Issue.Entries.OrderBy(entry => entry.SortOrder).ThenBy(entry => entry.Id))
                 {
+                    var issuedName = entry.ProjectReference?.Trim() ?? string.Empty;
+                    var linkedName = entry.ProjectName?.Trim() ?? string.Empty;
+                    var sameProjectName = entry.ProjectId.HasValue &&
+                        !string.IsNullOrWhiteSpace(linkedName) &&
+                        string.Equals(issuedName, linkedName, StringComparison.OrdinalIgnoreCase);
+
                     <tr class="@(entry.Category == ArppCategory.Delisted ? "delisted" : string.Empty)">
                         <td class="number">@entry.SortOrder</td>
-                        <td>@(entry.Category == ArppCategory.Delisted ? string.Empty : entry.SerialNumber)</td>
-                        <td>@(entry.Category == ArppCategory.Delisted ? string.Empty : entry.PppNumber)</td>
-                        <td>@entry.ProjectReference</td>
-                        <td>
-                            @if (entry.ProjectId.HasValue)
-                            {
-                                <span>@entry.ProjectName</span>
-                                @if (!string.IsNullOrWhiteSpace(entry.ProjectCaseFileNumber))
-                                {
-                                    <small>@entry.ProjectCaseFileNumber</small>
-                                }
-                                @if (!string.IsNullOrWhiteSpace(entry.ProjectStatus))
-                                {
-                                    <small>PRISM status: @entry.ProjectStatus</small>
-                                }
-                            }
-                            else
-                            {
-                                <span class="muted">Linkage pending</span>
+                        <td>@(entry.Category == ArppCategory.Delisted ? "—" : entry.SerialNumber)</td>
+                        <td>@(entry.Category == ArppCategory.Delisted ? "—" : entry.PppNumber)</td>
+                        <td class="arpp-print-project">
+                            <strong>@issuedName</strong>
+                            @if (entry.ProjectId.HasValue && !sameProjectName)
+                            {
+                                <small><b>Linked:</b> @linkedName</small>
+                            }
+                            @if (!string.IsNullOrWhiteSpace(entry.ProjectCaseFileNumber))
+                            {
+                                <small>@entry.ProjectCaseFileNumber</small>
+                            }
+                            @if (!string.IsNullOrWhiteSpace(entry.ProjectStatus))
+                            {
+                                <small>PRISM status: @entry.ProjectStatus</small>
+                            }
+                            else if (!entry.ProjectId.HasValue)
+                            {
+                                <small class="muted">PRISM linkage pending</small>
                             }
                         </td>
-                        <td>@ArppDisplayNames.For(entry.Category)</td>
-                        <td class="money">@IndianCurrencyFormatter.FormatRupees(entry.IpaCost).TrimStart('₹')</td>
+                        <td>@CategoryShortLabel(entry.Category)</td>
+                        <td class="money">@FormatPrintCurrency(entry.IpaCost).TrimStart('₹')</td>
                         <td>@entry.Cfa</td>
                         <td>@entry.Fund</td>
                         <td>@entry.DfpdsSchedule</td>
--- a/Pages/Projects/Arpp/Index.cshtml
+++ b/Pages/Projects/Arpp/Index.cshtml
@@ -93,7 +93,7 @@
                                value="@Model.Query"
                                type="search"
                                autocomplete="off"
-                               placeholder="Projects, documents or serials"
+                               placeholder="Search ARPP records"
                                title="Search document names, project references, PPP numbers, serial numbers, categories, CFA, Fund and DFPDS" />
                         @if (Model.Document is not null)
                         {
@@ -118,10 +118,10 @@
                                 class="project-arpp-search__submit"
                                 aria-label="Search ARPP library"
                                 title="Search">
-                            <i class="bi bi-arrow-right" aria-hidden="true"></i>
+                            <i class="bi bi-search" aria-hidden="true"></i>
                         </button>
                     </div>
-                    <small>Projects, issued wording, serial, category, CFA, Fund or DFPDS</small>
+                    <small>Projects, issued wording, PPP, serial, category, CFA, Fund or DFPDS</small>
                 </form>
 
                 <nav class="project-arpp-nav" aria-label="Published ARPP documents">
--- a/ProjectManagement.Tests/Arpp/ArppProductionFinishingContractTests.cs
+++ b/ProjectManagement.Tests/Arpp/ArppProductionFinishingContractTests.cs
@@ -0,0 +1,80 @@
+using Xunit;
+
+namespace ProjectManagement.Tests.Arpp;
+
+public sealed class ArppProductionFinishingContractTests
+{
+    [Fact]
+    public void PrintView_UsesProfessionalSummaryStatusAndNineColumnTable()
+    {
+        var markup = ReadRepoFile("Areas", "ProjectOfficeReports", "Pages", "ARPP", "Print.cshtml");
+        var css = ReadRepoFile("wwwroot", "css", "project-office-reports", "arpp-print.css");
+
+        Assert.Contains("arpp-print-summary__categories", markup, StringComparison.Ordinal);
+        Assert.Contains("Working-copy status:", markup, StringComparison.Ordinal);
+        Assert.Contains("Published position:", markup, StringComparison.Ordinal);
+        Assert.Contains("Revision @(Model.Issue.PublishedRevisionNumber", markup, StringComparison.Ordinal);
+        Assert.Contains("<col class=\"arpp-print-col--project\" />", markup, StringComparison.Ordinal);
+        Assert.DoesNotContain("<th>PRISM project</th>", markup, StringComparison.Ordinal);
+        Assert.Contains("grid-template-columns: repeat(4, minmax(0, 1fr));", css, StringComparison.Ordinal);
+        Assert.Contains(".arpp-print-table thead { display: table-header-group; }", css, StringComparison.Ordinal);
+    }
+
+    [Fact]
+    public void Workspace_KeepsIssuesCompactNavigatorCollapsibleAndActionsReachable()
+    {
+        var markup = ReadRepoFile("Areas", "ProjectOfficeReports", "Pages", "ARPP", "Manage.cshtml");
+        var css = ReadRepoFile("wwwroot", "css", "project-office-reports", "arpp-workspace.css");
+        var script = ReadRepoFile("wwwroot", "js", "pages", "project-office-reports", "arpp", "arpp-manage.js");
+
+        Assert.Contains("placeholder=\"Find row or project\"", markup, StringComparison.Ordinal);
+        Assert.Contains("arpp-workspace-v2__issues-toggle", markup, StringComparison.Ordinal);
+        Assert.Contains("Show or hide the row navigator", markup, StringComparison.Ordinal);
+        Assert.Contains(".arpp-workspace-v2.is-navigator-collapsed", css, StringComparison.Ordinal);
+        Assert.Contains(".arpp-workspace-v2__table .arpp-actions-cell", css, StringComparison.Ordinal);
+        Assert.Contains("position: sticky;", ExtractRule(css, ".arpp-workspace-v2__table .arpp-col-actions,"), StringComparison.Ordinal);
+        Assert.Contains("navigatorStorageKey", script, StringComparison.Ordinal);
+        Assert.Contains("restoreNavigatorLayout", script, StringComparison.Ordinal);
+        Assert.DoesNotContain("No unsaved changes ·", script, StringComparison.Ordinal);
+    }
+
+    [Fact]
+    public void ApplicationPages_RemoveRedundancyAndUseClearLabels()
+    {
+        var library = ReadRepoFile("Pages", "Projects", "Arpp", "Index.cshtml");
+        var register = ReadRepoFile("Areas", "ProjectOfficeReports", "Pages", "ARPP", "Index.cshtml");
+        var record = ReadRepoFile("Areas", "ProjectOfficeReports", "Pages", "ARPP", "Details.cshtml");
+
+        Assert.Contains("placeholder=\"Search ARPP records\"", library, StringComparison.Ordinal);
+        Assert.Contains("bi bi-search", library, StringComparison.Ordinal);
+        Assert.Contains("Active published records", register, StringComparison.Ordinal);
+        Assert.Contains("Records under work", register, StringComparison.Ordinal);
+        Assert.DoesNotContain("asp-page=\"/ARPP/Reconcile\" class=\"btn", register, StringComparison.Ordinal);
+        Assert.Contains("arpp-record-v2__identity-strip", record, StringComparison.Ordinal);
+    }
+
+    private static string ExtractRule(string css, string selector)
+    {
+        var start = css.IndexOf(selector, StringComparison.Ordinal);
+        if (start < 0) return string.Empty;
+        var end = css.IndexOf('}', start);
+        return end < 0 ? css[start..] : css[start..(end + 1)];
+    }
+
+    private static string ReadRepoFile(params string[] segments)
+    {
+        var root = ResolveRepoRoot();
+        return File.ReadAllText(Path.Combine(new[] { root }.Concat(segments).ToArray()));
+    }
+
+    private static string ResolveRepoRoot()
+    {
+        var current = new DirectoryInfo(AppContext.BaseDirectory);
+        while (current is not null)
+        {
+            if (File.Exists(Path.Combine(current.FullName, "ProjectManagement.csproj"))) return current.FullName;
+            current = current.Parent;
+        }
+        throw new DirectoryNotFoundException("Could not locate the ProjectManagement repository root.");
+    }
+}
--- a/ProjectManagement.Tests/Arpp/ArppUiUxStabilisationContractTests.cs
+++ b/ProjectManagement.Tests/Arpp/ArppUiUxStabilisationContractTests.cs
@@ -14,7 +14,8 @@
         Assert.Contains("data-arpp-save-state", markup, StringComparison.Ordinal);
         Assert.Contains("data-arpp-filter-count=\"issues\"", markup, StringComparison.Ordinal);
         Assert.Contains("baselineSnapshot = createFormSnapshot()", script, StringComparison.Ordinal);
-        Assert.Contains("No unsaved changes ·", script, StringComparison.Ordinal);
+        Assert.Contains("else state.textContent = \"No unsaved changes\";", script, StringComparison.Ordinal);
+        Assert.DoesNotContain("No unsaved changes ·", script, StringComparison.Ordinal);
         Assert.Contains("activeRowFilter", script, StringComparison.Ordinal);
         Assert.Contains("applyRowFilter", script, StringComparison.Ordinal);
         Assert.DoesNotContain("if (initialIssues.length || hasServerValidationErrors) markDirty()", script, StringComparison.Ordinal);
@@ -46,8 +47,8 @@
         Assert.Contains("Published revision @Model.Issue.PublishedRevisionNumber remains visible", record, StringComparison.Ordinal);
         Assert.Contains("@valueStateLabel approved value", record, StringComparison.Ordinal);
         Assert.Contains("@valueStateLabel delisted value", record, StringComparison.Ordinal);
-        Assert.Contains("Published positions", register, StringComparison.Ordinal);
-        Assert.Contains("Working copies", register, StringComparison.Ordinal);
+        Assert.Contains("Active published records", register, StringComparison.Ordinal);
+        Assert.Contains("Records under work", register, StringComparison.Ordinal);
         Assert.Contains("Verified and published", register, StringComparison.Ordinal);
         Assert.Contains("Unlocked for correction", register, StringComparison.Ordinal);
         Assert.Contains("Ready for verification", register, StringComparison.Ordinal);
--- a/wwwroot/css/project-office-reports/arpp-print.css
+++ b/wwwroot/css/project-office-reports/arpp-print.css
@@ -34,7 +34,7 @@
     width: min(100%, 297mm);
     min-height: 210mm;
     margin: 1rem auto;
-    padding: 12mm;
+    padding: 11mm 12mm;
     background: #fff;
     box-shadow: 0 3px 16px rgba(17, 24, 39, .16);
 }
@@ -67,12 +67,12 @@
 
 .arpp-print-summary {
     display: grid;
-    grid-template-columns: 22mm 40mm 40mm 1fr;
+    grid-template-columns: 22mm repeat(2, minmax(38mm, 1fr)) minmax(42mm, 1fr);
     gap: 2mm;
-    margin: 4mm 0;
-}
-
-.arpp-print-summary > div {
+    margin: 4mm 0 3mm;
+}
+
+.arpp-print-summary__metric {
     min-height: 17mm;
     padding: 2.5mm;
     border: 1px solid #c9d2dc;
@@ -91,34 +91,86 @@
     margin-top: 1mm;
     color: #17202a;
     font-size: 11pt;
-}
+    line-height: 1.15;
+}
+
+.arpp-print-summary__metric--approved strong { color: #146c43; }
+.arpp-print-summary__metric--delisted strong { color: #8a5a00; }
 
 .arpp-print-summary__categories {
-    display: grid;
-    grid-template-columns: repeat(2, minmax(0, 1fr));
-    gap: 1mm 3mm;
-}
-
-.arpp-print-summary__categories span { color: #17202a; }
+    grid-column: 1 / -1;
+    display: grid;
+    grid-template-columns: repeat(4, minmax(0, 1fr));
+    min-height: 0;
+    padding: 0;
+    overflow: hidden;
+    border: 1px solid #c9d2dc;
+    background: #fff;
+}
+
+.arpp-print-summary__categories > div {
+    display: grid;
+    grid-template-columns: auto auto minmax(0, 1fr);
+    align-items: baseline;
+    gap: 1.5mm;
+    min-width: 0;
+    padding: 2mm 2.5mm;
+    border-right: 1px solid #d7dee6;
+}
+
+.arpp-print-summary__categories > div:last-child { border-right: 0; }
+.arpp-print-summary__categories span {
+    color: #17365d;
+    font-weight: 700;
+}
+.arpp-print-summary__categories strong {
+    margin: 0;
+    font-size: 9pt;
+}
+.arpp-print-summary__categories small {
+    min-width: 0;
+    overflow: hidden;
+    text-align: right;
+    text-overflow: ellipsis;
+    white-space: nowrap;
+}
 
 .arpp-print-source {
+    display: grid;
+    gap: .7mm;
     margin-bottom: 3mm;
     padding: 2mm 2.5mm;
     border-left: 3px solid #17365d;
     background: #f7f9fb;
     font-size: 8pt;
+    line-height: 1.35;
+}
+
+.arpp-print-source__line {
+    min-width: 0;
+    overflow-wrap: anywhere;
 }
 
 .arpp-print-table {
     width: 100%;
     border-collapse: collapse;
     table-layout: fixed;
-    font-size: 7.4pt;
-}
+    font-size: 7.35pt;
+}
+
+.arpp-print-table col.arpp-print-col--number { width: 3%; }
+.arpp-print-table col.arpp-print-col--serial { width: 5%; }
+.arpp-print-table col.arpp-print-col--ppp { width: 14%; }
+.arpp-print-table col.arpp-print-col--project { width: 33%; }
+.arpp-print-table col.arpp-print-col--category { width: 8%; }
+.arpp-print-table col.arpp-print-col--cost { width: 10%; }
+.arpp-print-table col.arpp-print-col--cfa { width: 11%; }
+.arpp-print-table col.arpp-print-col--fund { width: 8%; }
+.arpp-print-table col.arpp-print-col--dfpds { width: 8%; }
 
 .arpp-print-table th,
 .arpp-print-table td {
-    padding: 1.5mm;
+    padding: 1.45mm 1.5mm;
     border: 1px solid #aeb9c5;
     vertical-align: top;
     overflow-wrap: anywhere;
@@ -131,20 +183,26 @@
     text-align: left;
 }
 
-.arpp-print-table th:nth-child(1) { width: 8mm; }
-.arpp-print-table th:nth-child(2) { width: 14mm; }
-.arpp-print-table th:nth-child(3) { width: 48mm; }
-.arpp-print-table th:nth-child(4) { width: 34mm; }
-.arpp-print-table th:nth-child(5) { width: 19mm; }
-.arpp-print-table th:nth-child(6) { width: 24mm; }
-.arpp-print-table th:nth-child(7) { width: 28mm; }
-.arpp-print-table th:nth-child(8) { width: 20mm; }
-.arpp-print-table th:nth-child(9) { width: 20mm; }
-
 .arpp-print-table tr.delisted td { background: #fff4d6; }
 .arpp-print-table .number { text-align: center; }
-.arpp-print-table .money { text-align: right; white-space: nowrap; font-variant-numeric: tabular-nums; }
-.arpp-print-table small { display: block; color: #526174; }
+.arpp-print-table .money {
+    text-align: right;
+    white-space: nowrap;
+    font-variant-numeric: tabular-nums;
+}
+.arpp-print-table small {
+    display: block;
+    margin-top: .6mm;
+    color: #526174;
+    font-size: 6.8pt;
+    line-height: 1.25;
+}
+.arpp-print-table .arpp-print-project > strong {
+    display: block;
+    color: #17202a;
+    font-weight: 600;
+    line-height: 1.3;
+}
 .muted { color: #6b7280; }
 
 .arpp-print-footer {
@@ -173,7 +231,11 @@
         padding: 0;
         box-shadow: none;
     }
+    .arpp-print-header { break-after: avoid; page-break-after: avoid; }
+    .arpp-print-summary,
+    .arpp-print-source { break-inside: avoid; page-break-inside: avoid; }
     .arpp-print-table thead { display: table-header-group; }
+    .arpp-print-table tfoot { display: table-footer-group; }
     .arpp-print-table tr { break-inside: avoid; page-break-inside: avoid; }
-    .arpp-print-footer { break-inside: avoid; }
-}
+    .arpp-print-footer { break-inside: avoid; page-break-inside: avoid; }
+}
--- a/wwwroot/css/project-office-reports/arpp-redesign.css
+++ b/wwwroot/css/project-office-reports/arpp-redesign.css
@@ -993,3 +993,37 @@
         transition: none;
     }
 }
+
+/* Production finishing refinements ------------------------------ */
+.arpp-record-v2__identity-strip {
+    display: flex;
+    flex-wrap: wrap;
+    gap: .45rem 1.2rem;
+    margin-top: .75rem;
+    padding: .58rem .72rem;
+    border-top: 1px solid var(--arpp-v2-border);
+    color: var(--arpp-v2-muted);
+    font-size: .75rem;
+}
+
+.arpp-record-v2__identity-strip span {
+    display: inline-flex;
+    align-items: baseline;
+    gap: .35rem;
+}
+
+.arpp-record-v2__identity-strip strong {
+    color: var(--bs-body-color);
+    font-size: .68rem;
+    font-weight: 700;
+    letter-spacing: .035em;
+    text-transform: uppercase;
+}
+
+@media (max-width: 767.98px) {
+    .arpp-record-v2__identity-strip {
+        display: grid;
+        grid-template-columns: 1fr;
+        gap: .35rem;
+    }
+}
--- a/wwwroot/css/project-office-reports/arpp-workspace.css
+++ b/wwwroot/css/project-office-reports/arpp-workspace.css
@@ -168,7 +168,9 @@
 }
 
 .arpp-workspace-v2__navigator-toggle {
-    display: none;
+    display: inline-flex;
+    align-items: center;
+    gap: .4rem;
 }
 
 .arpp-workspace-v2__search {
@@ -263,7 +265,11 @@
 }
 
 .arpp-workspace-v2__navigator-header .btn {
-    display: none;
+    display: inline-grid;
+    width: 2rem;
+    height: 2rem;
+    padding: 0;
+    place-items: center;
 }
 
 .arpp-workspace-v2__filters {
@@ -459,11 +465,11 @@
 }
 
 .arpp-workspace-v2__top-scrollbar {
-    height: .82rem;
+    height: .52rem;
     overflow-x: auto;
     overflow-y: hidden;
     border-bottom: 1px solid var(--arpp-workspace-border);
-    background: var(--bs-body-bg);
+    background: color-mix(in srgb, var(--bs-tertiary-bg) 58%, var(--bs-body-bg));
     scrollbar-color: color-mix(in srgb, var(--bs-secondary-color) 45%, transparent) transparent;
     scrollbar-width: thin;
 }
@@ -924,3 +930,74 @@
 .arpp-workspace-v2.has-validation:not(.is-dirty) .arpp-workspace-v2__save-state {
     color: var(--bs-warning-text-emphasis);
 }
+
+
+/* Production finishing refinements ------------------------------- */
+.arpp-workspace-v2__issues-toggle {
+    width: auto;
+    min-width: 6.2rem;
+    justify-content: center;
+    gap: .38rem;
+}
+
+.arpp-workspace-v2__issues-toggle .badge {
+    min-width: 1.35rem;
+}
+
+.arpp-workspace-v2.is-navigator-collapsed {
+    --arpp-workspace-sidebar: 0rem;
+}
+
+.arpp-workspace-v2.is-navigator-collapsed .arpp-workspace-v2__navigator {
+    display: none;
+}
+
+.arpp-workspace-v2__table .arpp-col-actions,
+.arpp-workspace-v2__table .arpp-actions-cell {
+    position: sticky;
+    right: 0;
+    border-left: 1px solid var(--arpp-workspace-border);
+    background: var(--bs-body-bg);
+    box-shadow: -.35rem 0 .65rem rgba(15, 23, 42, .045);
+}
+
+.arpp-workspace-v2__table .arpp-col-actions {
+    z-index: 10;
+    background: color-mix(in srgb, var(--bs-tertiary-bg) 94%, var(--bs-body-bg));
+}
+
+.arpp-workspace-v2__table .arpp-actions-cell {
+    z-index: 4;
+}
+
+.arpp-workspace-v2__table tbody tr:hover .arpp-actions-cell {
+    background: color-mix(in srgb, var(--bs-primary-bg-subtle) 18%, var(--bs-body-bg));
+}
+
+.arpp-workspace-v2__table tr.is-validation-target .arpp-actions-cell {
+    background: color-mix(in srgb, var(--bs-danger-bg-subtle) 34%, var(--bs-body-bg));
+}
+
+.arpp-workspace-v2.has-validation:not(.is-dirty) .arpp-workspace-v2__save-state {
+    color: var(--arpp-workspace-muted);
+    font-weight: 400;
+}
+
+@media (max-width: 1023.98px) {
+    .arpp-workspace-v2.is-navigator-collapsed .arpp-workspace-v2__navigator {
+        display: block;
+    }
+}
+
+@media (max-width: 767.98px) {
+    .arpp-workspace-v2__issues-toggle {
+        min-width: 2.5rem;
+    }
+
+    .arpp-workspace-v2__table .arpp-col-actions,
+    .arpp-workspace-v2__table .arpp-actions-cell {
+        position: static;
+        border-left: 0;
+        box-shadow: none;
+    }
+}
--- a/wwwroot/js/pages/project-office-reports/arpp/arpp-manage.js
+++ b/wwwroot/js/pages/project-office-reports/arpp/arpp-manage.js
@@ -28,6 +28,8 @@
     let jumpMatchIndex = -1;
     let scrollSyncInProgress = false;
     let activeRowFilter = "all";
+    const navigatorMedia = window.matchMedia("(max-width: 1023.98px)");
+    const navigatorStorageKey = "prism.arpp.workspace.navigatorCollapsed";
 
     const rows = () => Array.from(body.querySelectorAll("[data-arpp-entry-row]"));
     const getField = (row, suffix) => row.querySelector(`[name$=".${suffix}"]`);
@@ -84,9 +86,7 @@
         const state = root.querySelector("[data-arpp-save-state]");
         if (state) {
             if (saving) state.textContent = "Saving…";
-            else if (dirty && validationIssueCount > 0) state.textContent = `Unsaved changes · ${validationIssueCount} ${validationIssueCount === 1 ? "field requires" : "fields require"} attention`;
             else if (dirty) state.textContent = "Unsaved changes";
-            else if (validationIssueCount > 0) state.textContent = `No unsaved changes · ${validationIssueCount} ${validationIssueCount === 1 ? "field requires" : "fields require"} attention`;
             else state.textContent = "No unsaved changes";
         }
 
@@ -332,12 +332,53 @@
         if (closeNavigator && window.matchMedia("(max-width: 1023.98px)").matches) setNavigatorOpen(false);
     }
 
+    const setDesktopNavigatorCollapsed = collapsed => {
+        const shouldCollapse = Boolean(collapsed);
+        root.classList.toggle("is-navigator-collapsed", shouldCollapse);
+        root.querySelector("[data-arpp-navigator-toggle]")?.setAttribute("aria-expanded", shouldCollapse ? "false" : "true");
+        try {
+            window.localStorage.setItem(navigatorStorageKey, shouldCollapse ? "true" : "false");
+        } catch {
+            // Persistence is optional in private or hardened browser contexts.
+        }
+        window.requestAnimationFrame(syncTableScrollbars);
+    };
+
     const setNavigatorOpen = open => {
         const navigator = root.querySelector("[data-arpp-navigator]");
         const enabled = Boolean(open);
-        navigator?.classList.toggle("is-open", enabled);
-        root.classList.toggle("has-navigator-open", enabled);
-        root.querySelector("[data-arpp-navigator-toggle]")?.setAttribute("aria-expanded", enabled ? "true" : "false");
+        if (navigatorMedia.matches) {
+            navigator?.classList.toggle("is-open", enabled);
+            root.classList.toggle("has-navigator-open", enabled);
+            root.classList.remove("is-navigator-collapsed");
+            root.querySelector("[data-arpp-navigator-toggle]")?.setAttribute("aria-expanded", enabled ? "true" : "false");
+            return;
+        }
+
+        navigator?.classList.remove("is-open");
+        root.classList.remove("has-navigator-open");
+        setDesktopNavigatorCollapsed(!enabled);
+    };
+
+    const restoreNavigatorLayout = () => {
+        if (navigatorMedia.matches) {
+            root.classList.remove("is-navigator-collapsed");
+            root.querySelector("[data-arpp-navigator-toggle]")?.setAttribute(
+                "aria-expanded",
+                root.classList.contains("has-navigator-open") ? "true" : "false");
+            return;
+        }
+
+        root.querySelector("[data-arpp-navigator]")?.classList.remove("is-open");
+        root.classList.remove("has-navigator-open");
+        let collapsed = false;
+        try {
+            collapsed = window.localStorage.getItem(navigatorStorageKey) === "true";
+        } catch {
+            collapsed = false;
+        }
+        root.classList.toggle("is-navigator-collapsed", collapsed);
+        root.querySelector("[data-arpp-navigator-toggle]")?.setAttribute("aria-expanded", collapsed ? "false" : "true");
     };
 
     const setDetailsOpen = open => {
@@ -1122,7 +1163,13 @@
         jumpToMatch(true);
     });
     root.querySelector("[data-arpp-jump-next]")?.addEventListener("click", () => jumpToMatch(true));
-    root.querySelector("[data-arpp-navigator-toggle]")?.addEventListener("click", () => setNavigatorOpen(true));
+    root.querySelector("[data-arpp-navigator-toggle]")?.addEventListener("click", () => {
+        if (navigatorMedia.matches) {
+            setNavigatorOpen(true);
+            return;
+        }
+        setNavigatorOpen(root.classList.contains("is-navigator-collapsed"));
+    });
     root.querySelector("[data-arpp-navigator-close]")?.addEventListener("click", () => setNavigatorOpen(false));
     root.querySelector("[data-arpp-navigator-backdrop]")?.addEventListener("click", () => setNavigatorOpen(false));
     root.querySelectorAll("[data-arpp-details-toggle]").forEach(button => button.addEventListener("click", () => setDetailsOpen(true)));
@@ -1186,8 +1233,8 @@
     });
 
     window.addEventListener("resize", () => {
+        restoreNavigatorLayout();
         syncTableScrollbars();
-        if (!window.matchMedia("(max-width: 1023.98px)").matches) setNavigatorOpen(false);
     }, { passive: true });
 
     if (window.ResizeObserver) {
@@ -1218,6 +1265,7 @@
         });
     }
 
+    restoreNavigatorLayout();
     updateIssueSequence();
     reindexRows();
     syncTableScrollbars();
