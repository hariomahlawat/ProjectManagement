@page
@model ProjectManagement.Areas.Admin.Pages.TechnicalCategories.CreateModel
@using ProjectManagement.Areas.Admin.Models
@{
    ViewData["Title"] = "Add technical category";
    var header = new AdminPageHeaderModel
    {
        Eyebrow = "Master data · Technical categories",
        Title = "Add technical category",
        Description = "Create a technology classification for project portfolio and capability analysis.",
        Icon = "bi-node-plus",
        Actions = new[] { new AdminPageActionModel { Text = "Back to categories", Href = Url.Page("./Index"), Icon = "bi-arrow-left" } }
    };
}
<partial name="~/Areas/Admin/Pages/Shared/_AdminPageHeader.cshtml" model="header" />
<div class="admin-form-layout">
    <form method="post" class="admin-panel admin-form-panel" novalidate>
        <header class="admin-form-section__header"><span>01</span><div><h2>Technical classification</h2><p>Use a stable technology term that can support long-term reporting.</p></div></header>
        <div class="admin-form-section__body">
            <div asp-validation-summary="ModelOnly" class="admin-validation-summary" role="alert"></div>
            <div class="admin-form-grid admin-form-grid--two">
                <div class="admin-field admin-field--wide"><label asp-for="Input.Name"></label><input asp-for="Input.Name" class="form-control" maxlength="120" autocomplete="off" autofocus /><span asp-validation-for="Input.Name" class="text-danger"></span></div>
                <div class="admin-field"><label asp-for="Input.ParentId">Parent category</label><select asp-for="Input.ParentId" asp-items="Model.ParentOptions" class="form-select"></select><span asp-validation-for="Input.ParentId" class="text-danger"></span><small>Leave blank to create a root technology domain.</small></div>
            </div>
            <label class="admin-switch-row"><input asp-for="Input.IsActive" type="checkbox" /><span><strong>Active</strong><small>Make this classification available immediately.</small></span></label>
        </div>
        <footer class="admin-form-actions"><a asp-page="./Index" class="btn btn-outline-secondary">Cancel</a><button type="submit" class="btn btn-primary"><i class="bi bi-plus-lg"></i>Create category</button></footer>
    </form>
    <aside class="admin-guidance-card"><span class="admin-guidance-card__icon"><i class="bi bi-cpu"></i></span><h2>Taxonomy guidance</h2><ul><li>Use technology domains as root nodes.</li><li>Avoid vendor or product names.</li><li>Use child categories for specialised capabilities.</li></ul></aside>
</div>
@section Scripts { <partial name="_ValidationScriptsPartial" /> }
