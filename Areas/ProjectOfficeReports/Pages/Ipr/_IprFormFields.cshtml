@*
    Legacy compatibility placeholder.

    The former Manage page now redirects to the consolidated IPR Register editor,
    which is rendered by _RecordFormPartial.cshtml and uses IndexModel.

    This file intentionally renders no markup. Keeping the path as an empty Razor
    partial prevents stale deployments or cached references from causing Razor
    compilation failures while avoiding a second, divergent IPR form.
*@
