@model ProjectManagement.Areas.ProjectOfficeReports.Pages.Ipr.IndexModel
@using ProjectManagement.Application.Ipr

@foreach (var type in Model.Types)
{
    <input type="hidden" name="Types" value="@type" />
}
@foreach (var status in Model.Statuses)
{
    <input type="hidden" name="Statuses" value="@status" />
}
@if (Model.ProjectId.HasValue)
{
    <input type="hidden" name="ProjectId" value="@Model.ProjectId.Value" />
}
@if (Model.DateBasis != IprDateBasis.Filed || Model.Year.HasValue)
{
    <input type="hidden" name="DateBasis" value="@Model.DateBasis" />
}
@if (Model.Year.HasValue)
{
    <input type="hidden" name="Year" value="@Model.Year.Value" />
}
@if (Model.Linkage != IprLinkageFilter.All)
{
    <input type="hidden" name="Linkage" value="@Model.Linkage" />
}
@if (Model.Evidence != IprEvidenceFilter.All)
{
    <input type="hidden" name="Evidence" value="@Model.Evidence" />
}
