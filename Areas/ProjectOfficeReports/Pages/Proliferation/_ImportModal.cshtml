<form id="importForm" class="needs-validation" novalidate enctype="multipart/form-data">
    <div class="mb-3">
        <label class="form-label">Import type<span class="text-danger">*</span></label>
        <div class="btn-group" role="group" aria-label="Select import type">
            <input type="radio" class="btn-check" name="importType" id="import-type-yearly" value="yearly" autocomplete="off" checked>
            <label class="btn btn-outline-primary btn-sm" for="import-type-yearly">Yearly totals</label>
            <input type="radio" class="btn-check" name="importType" id="import-type-granular" value="granular" autocomplete="off">
            <label class="btn btn-outline-primary btn-sm" for="import-type-granular">Granular records</label>
        </div>
    </div>

    <div class="mb-3">
        <label for="import-file" class="form-label">CSV file<span class="text-danger">*</span></label>
        <div class="import-dropzone" role="button" tabindex="0" data-dropzone>
            <div class="import-dropzone__icon" aria-hidden="true"><i class="bi bi-cloud-arrow-up"></i></div>
            <p class="mb-1">Drag &amp; drop or click to upload</p>
            <p class="text-muted small mb-0">Accepted format: .csv (max 5 MB)</p>
            <input type="file" id="import-file" name="file" accept=".csv" required hidden />
        </div>
        <div class="invalid-feedback">Please select a CSV file using the official template.</div>
    </div>

    <div class="alert alert-warning d-flex align-items-start gap-2 small" role="note">
        <i class="bi bi-exclamation-triangle" aria-hidden="true"></i>
        <div>
            Imports are validated row by row. Errors are reported instantly without committing partial data.
        </div>
    </div>

    <div class="d-flex justify-content-between align-items-center mt-4">
        <div class="text-muted small" id="importStatus" aria-live="polite"></div>
        <div class="d-flex gap-2">
            <button type="button" class="btn btn-outline-secondary btn-sm" data-bs-dismiss="modal">Cancel</button>
            <button type="submit" class="btn btn-primary btn-sm" data-submit>
                <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true" hidden></span>
                Upload file
            </button>
        </div>
    </div>
</form>
