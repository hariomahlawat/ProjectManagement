@using System
@using ProjectManagement.Areas.ProjectOfficeReports.Proliferation.ViewModels

@{
    var toolbarModel = ProliferationFilterToolbarModel.Create(
        context: "overview",
        projects: Array.Empty<ProliferationFilterOption>(),
        sources: ProliferationFilterOption.FromPairs(new[]
        {
            ("1", "SDD"),
            ("2", "515 ABW")
        }),
        searchPlaceholder: "Search projects, units, or remarks"
    );
}

<partial name="_FilterToolbar" model="toolbarModel" />
