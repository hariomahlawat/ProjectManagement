<section id="entryWorkspace" class="mb-4" aria-labelledby="entry-workspace-heading">
    <div class="card border-0 shadow-sm rounded-4">
        <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-2">
                <div>
                    <h3 class="h6 text-uppercase text-muted mb-0">Records overview</h3>
                    <p class="text-muted small mb-0">
                        Totals may differ when yearly + granular entries exist.
                    </p>
                </div>
                <span class="badge rounded-pill text-bg-light" id="entryListCount" hidden>0 records</span>
            </div>

            <div id="tableContainer"
                 class="portfolio-records"
                 aria-live="polite"
                 aria-busy="true">
                <div class="text-center text-muted py-5">
                    Loading proliferation overview…
                </div>
            </div>

            <div class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center gap-2 mt-3"
                 id="entryPagination"
                 hidden>
                <div class="text-muted small" id="entryPagingSummary"></div>
                <nav aria-label="Proliferation pagination">
                    <ul class="pagination pagination-sm mb-0" id="entryPager"></ul>
                </nav>
            </div>
        </div>
    </div>
</section>
